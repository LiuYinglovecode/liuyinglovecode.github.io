<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java并发编程笔记</title>
    <url>/2020/01/29/Java%E5%B9%B6%E5%8F%91%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head><script type="text/javascript" src="http://cdn.mathjax.org/mathjax/latest/MathJax.js?config=default"></script>

</head><body><h3 id="01-可见性、原子性、有序性：并发的bug源头"><a href="#01-可见性、原子性、有序性：并发的bug源头" class="headerlink" title="01 可见性、原子性、有序性：并发的bug源头"></a>01 可见性、原子性、有序性：并发的bug源头</h3><ol>
<li>在 32 位的机器上对 long 型变量进行加减操作存在并发隐患？<br><br>long类型64位，所以在32位的机器上，对long类型的数据操作通常需要多条指令组合出来，无法保证原子性，所以并发的时候会出问题</li>
</ol>
<hr>
<ol start="2">
<li>进程与线程<br>进程不占有CPU。操作系统会把CPU分配给线程。分到CPU的线程就能执行。<br>并行，是同一时刻，两个线程都在执行。并发，是同一时刻，只有一个执行，但是一个时间段内，两个线程都执行了。</li>
</ol>
<hr>
<ol start="3">
<li>volatile</li>
</ol>
<ul>
<li>volatile 是一个类型修饰符。作为指令关键字，确保本条指令不会因编译器的优化而省略。</li>
<li>volatile 的特性:(1) 保证了不同线程对这个变量进行操作时的可见性，即一个线程修改了某个变量的值，这新值对其他线程来说是立即可见的。(2) 禁止进行指令重排序。（实现有序性）(3) volatile 只能保证对单次读/写的原子性。i++ 这种操作不能保证原子性。</li>
<li>volatile 的实现原理:(1) 变量的内存可见性是基于内存屏障（Memory Barrier）实现。内存屏障，又称内存栅栏，是一个 CPU 指令。<br>在程序运行时，为了提高执行性能，编译器和处理器会对指令进行重排序，JMM 为了保证在不同的编译器和 CPU 上有相同的结果，通过插入特定类型的内存屏障来禁止特定类型的编译器重排序和处理器重排序，插入一条内存屏障会告诉编译器和 CPU：不管什么指令都不能和这条 Memory Barrier 指令重排序。</li>
</ul>
<h3 id="02-Java内存模型：看Java如何解决可见性和有序性问题"><a href="#02-Java内存模型：看Java如何解决可见性和有序性问题" class="headerlink" title="02 Java内存模型：看Java如何解决可见性和有序性问题"></a>02 Java内存模型：看Java如何解决可见性和有序性问题</h3><ol>
<li>什么是Java内存模型<br>Java 内存模型规范了 JVM 如何提供按需禁用缓存和编译优化的方法。具体来说，这些方法包括 volatile、synchronized 和 final 三个关键字，以及六项 Happens-Before 规则，这也正是本期的重点内容<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class VolatileExample {</span><br><span class="line">  int x = 0;</span><br><span class="line">  volatile boolean v = <span class="literal">false</span>;</span><br><span class="line">  public void <span class="function"><span class="title">writer</span></span>() {</span><br><span class="line">    x = 42;</span><br><span class="line">    v = <span class="literal">true</span>;</span><br><span class="line">  }</span><br><span class="line">  public void <span class="function"><span class="title">reader</span></span>() {</span><br><span class="line">    <span class="keyword">if</span> (v == <span class="literal">true</span>) {</span><br><span class="line">      // 这里x会是多少呢？</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ol>
<ul>
<li>以上例子中，线程A执行writer方法并将V=true写入内存，B执行reader方法从内存中读取变量v,如果B看到v==true，那B看到的x是多少？<br>在1.5之前可能会出现x=0，因为CPU缓存的可见性问题。1.5之后已解决。具体就是对volatile的语义进行了增强——Happens-before规则。</li>
</ul>
<ol start="2">
<li>Happens-Before 规则：<br>前面一个操作的结果对后续操作是可见的。共有6项：(1) 程序的顺序性规则；（2） volatile 变量规则 （3） 传递性 （4） 管程中锁的规则：管程在java中指synchronized，指synchronized对Java里对管程的实现；<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line">synchronized (this) { //此处自动加锁</span><br><span class="line">  // x是共享变量,初始值=10</span><br><span class="line">  <span class="keyword">if</span> (this.x < 12) {</span><br><span class="line">    this.x = 12; </span><br><span class="line">  }  </span><br><span class="line">} //此处自动解锁</span><br></pre></td></tr></tbody></table></figure>
如上，管程中的锁在java里为隐式实现，在进入同步块前会自动加锁，执行完会自动释放锁，加锁和释放锁都是编译器实现的。所以结合规则 4——管程中锁的规则，可以这样理解：假设 x 的初始值是 10，线程 A 执行完代码块后 x 的值会变成 12（执行完自动释放锁），线程 B 进入代码块时，能够看到线程 A 对 x 的写操作，也就是线程 B 能够看到 x==12。这个也是符合直觉的。<br>（5） 线程start() 规则：主线程A启动子线程B后，子线程B可以看到主线程在启动子线程B前的操作。 （6） 线程join() 规则：主线程通过调用子线程B的join()并成功返回，主线程能看到子线程的操作。</li>
</ol>
<ul>
<li>被忽视的final规则</li>
<li>final 修饰变量时，初衷是告诉编译器：这个变量生而不变，可以优化。</li>
</ul>
<ol start="3">
<li>有一个共享变量 abc，在一个线程里设置了 abc 的值 abc=3，思考一下，有哪些办法可以让其他线程能够看到abc==3？<br>（1） 声明共享变量abc，并使用volatile关键字修饰abc；<br>（2） 声明共享变量abc，在synchronized关键字对abc的赋值代码块加锁，由于happen-before管程锁的规则，可以使得后续线程看到abc的值。<br>（3） A线程启动后，使用A.JOIN()方法来完成运行，后续线程再启动。</li>
</ol>
<ul>
<li>Java 内存模型主要分为两部分，一部分面向编写并发程序的应用开发人员，另一部分是面向 JVM 的实现人员的，也就是和编写并发程序相关的部分，这部分内容的核心就是 Happens-Before 规则。</li>
</ul>
<h3 id="03-互斥锁：解决原子性问题"><a href="#03-互斥锁：解决原子性问题" class="headerlink" title="03 互斥锁：解决原子性问题"></a>03 互斥锁：解决原子性问题</h3><p>如下图：<br><img src="../../images/3lock.png" alt="Alt text"></p>
<ul>
<li>保护资源R就得为它创建一把锁LR；</li>
<li>针对这把锁LR，需在进出临界区时添入加锁操作和解锁操作。</li>
</ul>
<ol>
<li>Java提供的锁技术：synchronized</li>
</ol>
<ul>
<li><p>synchronized可以用来修饰代码块：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class X {</span><br><span class="line">  // 修饰非静态方法</span><br><span class="line">  synchronized void <span class="function"><span class="title">foo</span></span>() {</span><br><span class="line">    // 临界区</span><br><span class="line">  }</span><br><span class="line">  // 修饰静态方法</span><br><span class="line">  synchronized static void <span class="function"><span class="title">bar</span></span>() {</span><br><span class="line">    // 临界区</span><br><span class="line">  }</span><br><span class="line">  // 修饰代码块</span><br><span class="line">  Object obj = new Object()；</span><br><span class="line">  void <span class="function"><span class="title">baz</span></span>() {</span><br><span class="line">    synchronized(obj) {</span><br><span class="line">      // 临界区</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>上式看到修饰静态方法的时候锁定了一个obj对象。此处有一个Java隐式规则：</p>
</li>
<li><p>当修饰静态方法的时候，锁定的是当前类的Class对象，在上面的例子即为Class X;当修饰非静态方法的时候，锁定的是当前实例对象this。</p>
</li>
<li><p>synchronized 修饰静态方法相当于：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class X {</span><br><span class="line">    //修饰静态方法</span><br><span class="line">    synchronized(X.class) static void <span class="function"><span class="title">bar</span></span>() {</span><br><span class="line">        //临界区</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>修饰非静态方法：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class X {</span><br><span class="line">    // 修饰非静态方法</span><br><span class="line">    synchronized(this) void <span class="function"><span class="title">foo</span></span>() {</span><br><span class="line">        //临界区</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ul>
<ol start="2">
<li>用synchronized解决count+=1 问题<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class SafeCalc {</span><br><span class="line">    long value = 0L;</span><br><span class="line">    long <span class="function"><span class="title">get</span></span>() {</span><br><span class="line">        <span class="built_in">return</span> value;</span><br><span class="line">    }</span><br><span class="line">    synchronized void <span class="function"><span class="title">addOne</span></span>() {</span><br><span class="line">        value += 1;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ol>
<ul>
<li>addOne()方法被修饰后，无论单核&多核CPU，只有一个线程能够执行它，所以可以保证原子操作。</li>
<li>由此引出的可见性问题，可以参考之前的Happens-Before，即前一个线程在临界区修改的共享变量（该操作在解锁之前），对后续进入临界区（该操作在加锁之后）的线程是可见的。所以多个线程同时执行addOne()方法是可以保证可见性的。</li>
<li>get()方法可见性是无法保证的。管程中锁的规则，是只保证后续对这个锁的加锁的可见性，而get()方法并没有加锁操作，解决方案：get()方法也synchronized：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class SafeCalc {</span><br><span class="line">    long value = 0L;</span><br><span class="line">    synchronized long <span class="function"><span class="title">get</span></span>() {</span><br><span class="line">        <span class="built_in">return</span> value;</span><br><span class="line">    }</span><br><span class="line">    synchronized void <span class="function"><span class="title">addOne</span></span>() {</span><br><span class="line">        value += 1;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ul>
<ol start="3">
<li><p>锁与受保护资源的关系</p>
</li>
<li><p>下面的代码用 synchronized 修饰代码块来尝试解决并发问题，这个使用方式正确吗？有哪些问题呢？能解决可见性和原子性问题吗？</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line">class SafeCalc {</span><br><span class="line">  long value = 0L;</span><br><span class="line">  long <span class="function"><span class="title">get</span></span>() {</span><br><span class="line">    synchronized (new Object()) {</span><br><span class="line">      <span class="built_in">return</span> value;</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">  void <span class="function"><span class="title">addOne</span></span>() {</span><br><span class="line">    synchronized (new Object()) {</span><br><span class="line">      value += 1;</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>答：</p>
</li>
</ol>
<ul>
<li>加锁本质就是在锁对象的对象头中写入当前线程id，但是new object每次在内存中都是新对象，所以加锁无效。</li>
<li>sync锁的对象monitor指针指向一个ObjectMonitor对象，所有线程加入他的entrylist里面，去cas抢锁，更改state加1拿锁，执行完代码，释放锁state减1，和aqs机制差不多，只是所有线程不阻塞，cas抢锁，没有队列，属于非公平锁。<br>wait的时候，线程进waitset休眠，等待notify唤醒</li>
<li>两把不同的锁，不能保护临界资源。而且这种new出来只在一个地方使用的对象，其它线程不能对它解锁，这个锁会被编译器优化掉。和没有syncronized代码块效果是相同的</li>
<li>不正确<br>使用锁保护资源时，对资源的所有操作应该使用同一个锁，这样才能起到保护的作用。<br>课后题中每个线程对资源的操作都是用的是各自的锁，不存在互斥和竞争的情况。<br>这就相当于有一个房间，每个人过来都安装一个门，每个人都有自己门的钥匙，大家都可以随意出入这个房间。<br>由于每个线程都可以随时进入方法，所以存在原子性问题；<br>但是因为每次都有加锁和解锁的操作，unlock操作会使其他缓存的变量失效，需要重新从主内存中加载变量的值，所以可以解决可见性问题。</li>
</ul>
<h3 id="04-互斥锁：如何用一把锁保护多个资源？"><a href="#04-互斥锁：如何用一把锁保护多个资源？" class="headerlink" title="04 互斥锁：如何用一把锁保护多个资源？"></a>04 互斥锁：如何用一把锁保护多个资源？</h3><ol>
<li>代码演示<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Account {</span><br><span class="line">  // 锁：保护账户余额</span><br><span class="line">  private final Object balLock</span><br><span class="line">    = new Object();</span><br><span class="line"></span><br><span class="line">  private Integer balance;</span><br><span class="line">  // 锁：保护账户密码</span><br><span class="line">  private final Object pwLock</span><br><span class="line">    = new Object();</span><br><span class="line"></span><br><span class="line">  private String password;</span><br><span class="line"></span><br><span class="line">   void withdraw(Integer amt) {</span><br><span class="line">    synchronized(balLock) {</span><br><span class="line">      <span class="keyword">if</span> (this.balance > amt){</span><br><span class="line">        this.balance -= amt;</span><br><span class="line">      }</span><br><span class="line">    }</span><br><span class="line">  } </span><br><span class="line"> </span><br><span class="line">  Integer <span class="function"><span class="title">getBalance</span></span>() {</span><br><span class="line">    synchronized(balLock) {</span><br><span class="line">      <span class="built_in">return</span> balance;</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line"></span><br><span class="line">  void updatePassword(String pw){</span><br><span class="line">    synchronized(pwLock) {</span><br><span class="line">      this.password = pw;</span><br><span class="line">    }</span><br><span class="line">  } </span><br><span class="line"></span><br><span class="line">  String <span class="function"><span class="title">getPassword</span></span>() {</span><br><span class="line">    synchronized(pwLock) {</span><br><span class="line">      <span class="built_in">return</span> password;</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ol>
<ul>
<li>account有成员变量balance & password，设了两把锁balLock & pwLock.不同的资源用不同的锁。</li>
<li>也可以用互斥锁来保护多个资源，e.g.可以用this管理账户里的所有资源，在这里即指余额和密码。实现：所有程序都加同步关键字synchronized。</li>
<li>用一把锁性能又差，会导致上面四个操作都是串行的。所以用不同的锁对受保护资源进行精细化管理，能够提升性能，这种锁即是——细粒度锁。</li>
</ul>
<ol start="2">
<li>保护有关联关系的多个资源<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Account {</span><br><span class="line">  private int balance;</span><br><span class="line">  // 转账</span><br><span class="line">  synchronized void transfer(</span><br><span class="line">      Account target, int amt){</span><br><span class="line">    <span class="keyword">if</span> (this.balance > amt) {</span><br><span class="line">      this.balance -= amt;</span><br><span class="line">      target.balance += amt;</span><br><span class="line">    }</span><br><span class="line">  } </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ol>
<ul>
<li>临界区有俩资源，转出账户的余额this.balance和转入账户的余额target.balance,并且用了一把锁this。</li>
<li>错误之处在于：this这把锁只可以保护自己的余额this.balance,无法保护target.balance.<br>如图所示：<br><img src="../../images/thisbalance.png" alt="Alt text"></li>
<li>假设ABC同时有200，A转B100，B转C100，那么：</li>
</ul>
<p>最终进入B的余额可能是：<br>（1）300：线程1后于2写入B.balance，线程2写的B.balance值被线程1覆盖；<br>（2）100：线程1先于2写入B.balance，线程1写的B.balance值被线程2覆盖；</p>
<ol start="3">
<li>正确用锁</li>
</ol>
<ul>
<li>用一把锁来保护多个资源，要求锁能覆盖所有受保护资源。上个例子中的this是对象级别的锁，所以A对象和B对象都有自己的锁，那么如何使其共享？</li>
</ul>
<ol start="4">
<li>共享方案——设细粒度更大的锁</li>
</ol>
<ul>
<li><p>让所有对象都持有一个唯一性的对象，这个对象在创建Account时传入：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Account {</span><br><span class="line">    private Object lock;</span><br><span class="line">    private int balance;</span><br><span class="line">    private Account();</span><br><span class="line">    // 创建account时传入同一个lock对象</span><br><span class="line">    public Account(Object lock) {</span><br><span class="line">        this.lock = lock;</span><br><span class="line">    }</span><br><span class="line">    // 转账</span><br><span class="line">    void transfer(Account target, int amt) {</span><br><span class="line">        //此处检查所有对象共享的锁</span><br><span class="line">        synchronized(lock) {</span><br><span class="line">            <span class="keyword">if</span>(this.balance > amt) {</span><br><span class="line">                this.balance -= amt;</span><br><span class="line">                target.balance += amt;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
<li><p>如上，将account默认构造函数变为private，同时增加一个带Object lock参数的构造函数，创建account对象时，传入相同的lock，这样所有的account对象都会共享这个lock，如下图所示：<br><img src="../../images/sharelock.png" alt="Alt text"></p>
</li>
<li><p>再解“原子性”：本质不是不可分割，不可分割只是外在表现，其本质是多个资源间有一致性的要求，操作的中间状态对外不可见。e.g.在 32 位的机器上写 long 型变量有中间状态（只写了 64 位中的 32 位），在银行转账的操作中也有中间状态（账户 A 减少了 100，账户 B 还没来得及发生变化）。所以解决原子性问题，是要保证中间状态对外不可见。</p>
</li>
</ul>
<ol start="5">
<li>思考<br>在第一个示例程序里，用了两把不同的锁来分别保护账户余额、账户密码，创建锁的时候，我们用的是：private final Object xxxLock = new Object();，如果账户余额用 this.balance 作为互斥锁，账户密码用 this.password 作为互斥锁，是否可以呢？</li>
</ol>
<ul>
<li>都不行。不能用可变对象做锁。A线程取款进行this.balance-=amt时，this.balance对应的值已经发生变换，线程B再次取款时拿到的balance对应的值并不是A线程中的。</li>
<li>不能用balance和password做为锁对象。这两个对象balance是Integer，password是String都是不可变变对象，一但对他们进行赋值就会变成新的对象，加的锁就失效了。</li>
<li>举个例子，假如this.balance = 10 ，多个线程同时竞争同一把锁this.balance，此时只有一个线程拿到了锁，其他线程等待，拿到锁的线程进行this.balance -= 1操作，this.balance = 9。 该线程释放锁， 之前等待锁的线程继续竞争this.balance=10的锁，新加入的线程竞争this.balance=9的锁，导致多个锁对应一个资源。</li>
<li>在account中添加一个静态object，通过锁这个object来实现一个锁保护多个资源：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Account {</span><br><span class="line">    private static Object lock = new Object()</span><br><span class="line">    private int balance;</span><br><span class="line">    // 转账</span><br><span class="line">    void transfer(Account target, int amt) {</span><br><span class="line">        synchronized(lock) {</span><br><span class="line">            <span class="keyword">if</span>(this.balance > amt) {</span><br><span class="line">                this.balance -= amt;</span><br><span class="line">                target.balance += amt;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
这种方式比锁class更安全，因为这个缺是私有的，有些最佳实践必须这样做。</li>
<li>引申到下一讲：然后感觉加锁的所有用户用同一个锁的粒度太大了，但如果每次转账操作，是不是可以同时加两个用户的锁，如果有先后顺序又可能有死锁问题。</li>
</ul>
<h3 id="05-死锁"><a href="#05-死锁" class="headerlink" title="05 死锁"></a>05 死锁</h3><ul>
<li>之前一篇用Account.class作为互斥锁的方案虽然不存在并发问题，但是所有账户的转账操作都是串行的，性能太差。</li>
</ul>
<ol>
<li>提升性能方案</li>
</ol>
<ul>
<li>用两把锁：转出账本一把，转入账本另一把。如图：<br><img src="../../images/solvedeadlock.png" alt="Alt text"><br>在transfer()方法内部，首先锁定this(先把转出账本拿到)，然后尝试锁定转入账户target(再把转入账本拿到)，只有两者都成功时，才执行转账操作。</li>
</ul>
<ol start="2">
<li>代码<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Account {</span><br><span class="line">    private transfer(Account target, int amt) {</span><br><span class="line">        // 锁定转出账户</span><br><span class="line">        synchronized(this) {           ①</span><br><span class="line">            // 锁定转入账户</span><br><span class="line">            synchronized(target) {     ②</span><br><span class="line">                <span class="keyword">if</span>(this.balance > amt) {</span><br><span class="line">                    this.balance -= amt;</span><br><span class="line">                    target.balance += amt;</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line"></span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
此例相对于用Account.class作为互斥锁，锁定的范围太大，而锁定俩个账户范围就小多了，这样的锁就是之前介绍的细粒度锁。细粒度锁可以提高并行度，是性能优化的重要手段。</li>
</ol>
<ul>
<li>使用细粒度锁的代价——死锁<br>一组互相竞争资源的线程因互相等待，导致“永久”阻塞的现象<br>假设：线程T1：A转账B；线程T2: B转账A；当两线程执行完①时，T1拿到A的锁，T2拿到B的锁。之后俩线程执行②时，T1要B的锁发现B已被T2锁定，T2亦如是。这时就会无限等待下去。。。</li>
</ul>
<ol start="3">
<li>预防</li>
</ol>
<ul>
<li>出现原因：互斥（无法破坏，用锁的目的就是互斥）、占有且等待、不可抢占、循环等待。</li>
<li>解决：</li>
</ul>
<p>3.1 对于“占有且等待”：可以一次性申请所有的资源。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Allocator {</span><br><span class="line">    private List<Object> als =</span><br><span class="line">      new ArrayList<>();</span><br><span class="line">      // 一次申请所有资源</span><br><span class="line">      synchronized boolean apply(</span><br><span class="line">          Object from, Object to) {</span><br><span class="line">              <span class="keyword">if</span>(als.contains(from) ||</span><br><span class="line">              als.contains(to)){</span><br><span class="line">                  <span class="built_in">return</span> <span class="literal">false</span>;</span><br><span class="line">              } <span class="keyword">else</span> {</span><br><span class="line">                  als.add(from);</span><br><span class="line">                  als.add(to);</span><br><span class="line">              }</span><br><span class="line">              <span class="built_in">return</span> <span class="literal">true</span>;</span><br><span class="line">          }</span><br><span class="line">          // 归还资源</span><br><span class="line">          synchronized void free(</span><br><span class="line">              Object from, Object to){</span><br><span class="line">                  als.remove(from);</span><br><span class="line">                  als.remove(to);</span><br><span class="line">              }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">class Account {</span><br><span class="line">    // actr 应为单例</span><br><span class="line">    private Allocator actr;</span><br><span class="line">    private int balance;</span><br><span class="line">    // 转账</span><br><span class="line">    void transfer(Account target, int amt) {</span><br><span class="line">        // 一次性申请转出账户和转入转入账户，直到成功</span><br><span class="line">        <span class="keyword">while</span>(!actr.apply(this, target))</span><br><span class="line">        ;</span><br><span class="line">        try{</span><br><span class="line">            // 锁定转出账户</span><br><span class="line">            synchronized(target){</span><br><span class="line">                // 锁定转入账户</span><br><span class="line">                synchronized(target){</span><br><span class="line">                    <span class="keyword">if</span> (this.balance > amt){</span><br><span class="line">                        this.balance -= amt;</span><br><span class="line">                        target.balance += amt;</span><br><span class="line">                    }</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        } finally {</span><br><span class="line">            actr.free(this, target)</span><br><span class="line">        }</span><br><span class="line">      </span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>3.2 对于”不可抢占“：占用部分资源的线程申请其他资源而不得时主动释放它占有的资源。</p>
<ul>
<li>破坏不可抢占条件看上去很简单，核心是要能够主动释放它占有的资源，这一点 synchronized 是做不到的。原因是 synchronized 申请资源的时候，如果申请不到，线程直接进入阻塞状态了，而线程进入阻塞状态，啥都干不了，也释放不了线程已经占有的资源。Java 在语言层次没有解决这个问题，不过在 SDK 层面还是解决了的，java.util.concurrent 这个包下面提供的 Lock 是可以轻松解决这个问题的。关于这个话题，后面会详细讲。</li>
</ul>
<p>3.3 对于”循环等待“： 按序申请。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class Account {</span><br><span class="line">    private int id;</span><br><span class="line">    private int balance;</span><br><span class="line">    //转账</span><br><span class="line">    void transfer(Account target, int amt) {</span><br><span class="line">        Account left = this            ①</span><br><span class="line">        Account right = target;        ②</span><br><span class="line">        <span class="keyword">if</span> (this.id > target.id) {     ③</span><br><span class="line">            left = target;             ④</span><br><span class="line">            right = this;              ⑤</span><br><span class="line">        }</span><br><span class="line">        // 锁定序号小的账户</span><br><span class="line">        synchronized(left){</span><br><span class="line">            // 锁定序号大的账户</span><br><span class="line">            synchronized(right){</span><br><span class="line">                <span class="keyword">if</span>(this.balance > amt){</span><br><span class="line">                    this.balance -= amt;</span><br><span class="line">                    target.balance += amt;</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        }                              ⑥</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ol start="4">
<li>思考<br>上面提到：破坏占用且等待条件，也是锁了所有的账户，而且还是用了死循环 while(!actr.apply(this, target));这个方法，那它比 synchronized(Account.class) 有没有性能优势呢？</li>
</ol>
<ul>
<li>synchronized(Account.class) 锁了Account类相关的所有操作。相当于文中说的包场了，只要与Account有关联，通通需要等待当前线程操作完成。while死循环的方式只锁定了当前操作的两个相关的对象。两种影响到的范围不同。</li>
<li>while循环可以应该计入timeoout，避免一直阻塞。</li>
<li>在实际开发中都是用数据库事务+乐观锁的方式进行解决。以上为例子，为了说明死锁原理。</li>
<li>最常见的就是B转A的同时，A转账给B，那么先锁B再锁A，但是，另一个线程是先锁A再锁B，然而，如果两个线程同时执行，那么就是出现死锁的情况，线程T1锁了A请求锁B，此时线程T2锁了B请求锁A，都在等着对方释放锁，然而自己都不会释放锁，故死锁。<br>最简单的办法，就是无论哪个线程执行的时候，都按照顺序加锁，即按照A和B的id大小来加锁，这样，无论哪个线程执行的时候，都会先加锁A，再加锁B，A被加锁，则等待释放。这样就不会被死锁了。</li>
</ul>
<h3 id="06-“等待-通知”机制优化循环等待"><a href="#06-“等待-通知”机制优化循环等待" class="headerlink" title="06 “等待-通知”机制优化循环等待"></a>06 “等待-通知”机制优化循环等待</h3><ol>
<li>回顾：破坏占用且等待条件时，如转出账本和转入账本不满足同时在文件架上这个条件，就用死循环的方式来循环等待，核心代码如下：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">// 一次性申请转出账户和转入账户，直到成功</span><br><span class="line"><span class="keyword">while</span>(!actr.apply(this, target))</span><br><span class="line">  ；</span><br></pre></td></tr></tbody></table></figure>
apply()操作耗时非常短且并发冲突量也不大时该方案可行，但耗时长&并发冲突量大时太消耗CPU了。</li>
</ol>
<ul>
<li>解决方案：如果线程要求的条件（转出账本和转入账本同在文件架上）不满足，则线程阻塞自己，进入等待状态；当线程要求的条件（转出账本和转入账本同在文件架上）满足后，通知等待的线程重新执行。其中，使用线程阻塞的方式就能避免循环等待消耗 CPU 的问题。</li>
</ul>
<ol start="2">
<li>synchronized 实现等待-通知机制</li>
</ol>
<ul>
<li>synchronized配合wait(),notify(),notifyAll()实现。<br><img src="../../images/notify.png" alt="Alt text"></li>
<li>wait()、notify()、notifyAll() 都是在 synchronized{}内部被调用的。如果在 synchronized{}外部调用，或者锁定的 this，而用 target.wait() 调用的话，JVM 会抛出一个运行时异常：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">java.lang.IllegalMonitorStateException。</span><br></pre></td></tr></tbody></table></figure></li>
</ul>
<ol start="3">
<li>等待-通知机制的四个要素：</li>
</ol>
<ul>
<li>(1)互斥锁：上一篇提到 Allocator 需要是单例的，所以用 this 作为互斥锁。</li>
<li>(2)线程要求的条件：转出账户和转入账户都没有被分配过。</li>
<li>(3)何时等待：线程要求的条件不满足就等待。</li>
<li>(4)何时通知：当有线程释放账户时就通知。<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(条件不满足) {</span><br><span class="line">  <span class="built_in">wait</span>();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
利用该范式可以解决上面提到的条件曾经满足过这个问题。因为当 wait() 返回时，有可能条件已经发生变化了，曾经条件满足，但是现在已经不满足了，所以要重新检验条件是否满足。范式，意味着是经典做法，所以没有特殊理由不要换写法。后面在介绍“管程”的时候，会详细介绍这个经典做法的前世今生。<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line">class Allocator {</span><br><span class="line">  private List<Object> als;</span><br><span class="line">  // 一次性申请所有资源</span><br><span class="line">  synchronized void apply(</span><br><span class="line">    Object from, Object to){</span><br><span class="line">    // 经典写法</span><br><span class="line">    <span class="keyword">while</span>(als.contains(from) ||</span><br><span class="line">         als.contains(to)){</span><br><span class="line">      try{</span><br><span class="line">        <span class="built_in">wait</span>();</span><br><span class="line">      }catch(Exception e){</span><br><span class="line">      }   </span><br><span class="line">    } </span><br><span class="line">    als.add(from);</span><br><span class="line">    als.add(to);  </span><br><span class="line">  }</span><br><span class="line">  // 归还资源</span><br><span class="line">  synchronized void free(</span><br><span class="line">    Object from, Object to){</span><br><span class="line">    als.remove(from);</span><br><span class="line">    als.remove(to);</span><br><span class="line">    notifyAll();</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
所以除非经过深思熟虑，否则尽量使用 notifyAll()。</li>
</ul>
<ol start="4">
<li>wait() 方法和 sleep() 方法都能让当前线程挂起一段时间，那它们的区别是什么？</li>
</ol>
<ul>
<li>wait与sleep区别在于：<br>wait会释放所有锁而sleep不会释放锁资源.<br>wait只能在同步方法和同步块中使用，而sleep任何地方都可以.<br>wait无需捕捉异常，而sleep需要.<br>sleep是Thread方法，wait是Object类方法；sleep调用时必须指定时间。<br>两者相同点：都会让渡CPU执行时间，等待再次调度！</li>
<li>代码：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">public class MyLock {</span><br><span class="line">  // 测试转账main方法</span><br><span class="line">  public static void main(String[] args) throws InterruptedException {</span><br><span class="line">    Account src = new Account(10000);</span><br><span class="line">    Account target = new Account(10000);</span><br><span class="line">    CountDownLatch countDownLatch = new CountDownLatch(9999);</span><br><span class="line">    <span class="keyword">for</span> (int i = 0; i < 9999; i++) {</span><br><span class="line">      new Thread(0->{</span><br><span class="line">        src.transactionToTarget(1,target);</span><br><span class="line">        countDownLatch.countDown();</span><br><span class="line">      }).start();</span><br><span class="line">    }</span><br><span class="line">    countDownLatch.await();</span><br><span class="line">    System.out.println(<span class="string">"src="</span>+src.getBalance());</span><br><span class="line">    System.out.println(<span class="string">"target="</span>+target.getBalance());</span><br><span class="line">  }</span><br><span class="line">  static class Account{//账户类</span><br><span class="line">  public Account(Integer balance){</span><br><span class="line">    this.balance = balance;</span><br><span class="line">  }</span><br><span class="line">  private Integer balance;</span><br><span class="line">  public void transactionToTarget(Integer money,Account target){//转账方法</span><br><span class="line">  Allocator.getInstance().apply(this,target);</span><br><span class="line">  this.balance -= money;</span><br><span class="line">  target.setBalance(target.getBalance()+money);</span><br><span class="line">  Allocator.getInstance().release(this,target);</span><br><span class="line">  }</span><br><span class="line">  public Integer <span class="function"><span class="title">getBalance</span></span>(){</span><br><span class="line">    <span class="built_in">return</span> balance;</span><br><span class="line">  }</span><br><span class="line">  public void setBalance(Integer balance){</span><br><span class="line">    this.balance = balance;</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line">static class Allocator{//单例锁类</span><br><span class="line">  private <span class="function"><span class="title">Allocator</span></span>(){}</span><br><span class="line">  private List<Account> locks = new ArrayList<>();</span><br><span class="line">  public synchronized void apply(Account src,Account tag){</span><br><span class="line">    <span class="keyword">while</span> (locks.contains(src)||locks.contains(tag)){</span><br><span class="line">      try{</span><br><span class="line">        this.wait();</span><br><span class="line">      } catch(InterruptedException e) {</span><br><span class="line">      }</span><br><span class="line">    }</span><br><span class="line">    locks.add(src);</span><br><span class="line">    locks.add(tag);</span><br><span class="line">  }</span><br><span class="line">  public synchronized void release(Account src,Account tag){</span><br><span class="line">    locks.remove(src);</span><br><span class="line">    locks.remove(tag);</span><br><span class="line">    this.notifyAll();</span><br><span class="line">  }</span><br><span class="line">  public static Allocator <span class="function"><span class="title">getInstance</span></span>(){</span><br><span class="line">    <span class="built_in">return</span> AllocatorSingle.install;</span><br><span class="line">  }</span><br><span class="line">  static class AllocatorSingle{</span><br><span class="line">    public static Allocator install = new Allocator();</span><br><span class="line">  }</span><br><span class="line">  }</span><br><span class="line">  }</span><br></pre></td></tr></tbody></table></figure>

</li>
</ul>
<h3 id="07-安全、活跃、性能"><a href="#07-安全、活跃、性能" class="headerlink" title="07 安全、活跃、性能"></a>07 安全、活跃、性能</h3><ol>
<li>安全性问题</li>
</ol>
<ul>
<li>现实中，必须共享会发生变化的数据。<br>当多个线程同时访问同一数据并至少有一个线程会写这个数据的时候，若不采取措施就会导致并发bug，即数据竞争（data race).<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">public class Test {</span><br><span class="line">  private long count = 0;</span><br><span class="line">  synchronized long <span class="function"><span class="title">get</span></span>(){</span><br><span class="line">    <span class="built_in">return</span> count;</span><br><span class="line">  }</span><br><span class="line">  synchronized void <span class="built_in">set</span>(long v){</span><br><span class="line">    count = v;</span><br><span class="line">  }</span><br><span class="line">  void <span class="function"><span class="title">add10K</span></span>() {</span><br><span class="line">    int idx = 0;</span><br><span class="line">    <span class="keyword">while</span>(idx++ < 10000) {</span><br><span class="line">      <span class="built_in">set</span>(get()+1)</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
<li>竟态条件(race condition)<br>指程序的执行结果以来线程执行的顺序。如：<br>当两个线程同时执行get()时，get()方法会返回相同的值0(假设count=0),再执行get()+1操作，结果都是1，本来期望2，而结果都是1。<br>在并发环境里，线程的执行顺序时不确定的，如果存在竟态条件问题，那就意味着程序的执行的结果是不确定的。<br>解决数据竞争和竟态条件问题，需要：锁。</li>
</ul>
<ol start="2">
<li><p>活跃性问题<br>活跃性指的是某个操作无法执行下去，包括：死锁、活锁、饥饿。活锁指的是俩线程“谦让”，每次谦让的条件相同，导致又相遇。解决方案就是设立随机时间等待。饥饿指的是线程因无法访问所需资源而无法执行下去的情况（线程优先级低）。解决方法：公平锁。</p>
</li>
<li><p>性能问题</p>
</li>
</ol>
<ul>
<li>锁的使用会带来性能问题。<br>过度使用锁会导致串行范围过大，这样就不能发挥多线程的优势了。所以要尽量减少串行。</li>
<li>阿姆达尔Amdahl公式<br>代表了处理器并行运行之后效率提升的能力：<br>$$S=\frac{1}{(1-p)+\frac{p}{n}}$$<br>n为cpu核数，p为并行百分比，(1-p)为串行百分比（即5%），再假设cpu核数无穷大，那加速比s的极限就是20，所以，如果串行率是5%，那么无论采用什么技术，最高也就提升20倍的性能。<ul>
<li>方案<br>（1）既然使用锁会带来性能问题，那最好的方案就是使用无锁的算法和数据结构。相关技术：线程本地存储(Thread Local Storage,TLS)、写入时复制(Copy-on-write)、乐观锁等；Java并发包里面的原子类也是一种无锁的数据结构；Disruptor则是一个无锁的内存队列，性能非常好。<br>(2) 减少锁持有的时间。互斥锁本质上是将并行的程序串行化，所以要增加并行度，一定要减少持有锁的时间。实现技术：使用细粒度的锁，一个典型的例子就是Java并发包里的ConcurrentHashMap，它使用了所谓分段锁的技术（这个技术后面会详细介绍);还可以使用读写锁，也就是读是无锁的，只有写的时候才会互斥。</li>
<li>性能指标：<br>(1) 吞吐量：指单位时间内能处理的请求数量。<br>(2) 延迟：指的是从发出请求到收到响应的时间。<br>(3) 并发量：同时处理的请求数量。</li>
</ul>
</li>
</ul>
<ol start="4">
<li><p>总结<br>并发编程是一个复杂的技术领域，微观上涉及到原子性问题、可见性问题和有序性问题，宏观则表现为安全性、活跃性以及性能问题。我们在设计并发程序的时候，主要是从宏观出发，也就是要重点关注它的安全性、活跃性以及性能。安全性方面要注意数据竞争和竞态条件，活跃性方面需要注意死锁、活锁、饥饿等问题，性能方面我们虽然介绍了两个方案，但是遇到具体问题，你还是要具体分析，根据特定的场景选择合适的数据结构和算法。要解决问题，首先要把问题分析清楚。同样，要写好并发程序，首先要了解并发程序相关的问题，</p>
</li>
<li><p>思考<br>Java语言提供的vector是一个线程安全的容器，判断：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">void addIfNotExist(Vector v,</span><br><span class="line">    Object o) {</span><br><span class="line">      <span class="keyword">if</span>(!v.contains(o)){</span><br><span class="line">        v.add(o);</span><br><span class="line">      }</span><br><span class="line">    }</span><br></pre></td></tr></tbody></table></figure></li>
</ol>
<ul>
<li>vector 是线程安全，指的是其方法执行时么有并发正确性问题，并不代表其操作组合在一起没问题，此程序有竟态条件问题。</li>
<li>vector实现线程安全是通过给主要的写方法加了synchronized，类似contains这样的读方法并没有synchronized，该题的问题出在不是线程安全的contains方法，两个线程如果同时执行到if(!v.contains(o))是可以通过的，这时就会执行两次add方法，重复添加，即竟态条件。</li>
<li>串行百分比可以这么理解：临界区都是串行的，非临界区都是并行的，用单线程执行临界区的时间/用单线程执行（临界区+非临界区）的时间就时串行百分比。</li>
<li>修改：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">void addIfNotExist(Vector v,</span><br><span class="line">    Object o){</span><br><span class="line">      synchronized(v){</span><br><span class="line">        <span class="keyword">if</span>(!v.contains(o)){</span><br><span class="line">          v.add(o);</span><br><span class="line">        }</span><br><span class="line">      }</span><br><span class="line">    }</span><br></pre></td></tr></tbody></table></figure>
<h3 id="08-管程"><a href="#08-管程" class="headerlink" title="08 管程"></a>08 管程</h3></li>
</ul>
<ol>
<li>什么是管程<br>管程(monitor)是管理共享变量以及对共享变量的操作过程，让他们支持并发。在Java中即为管理类的成员变量和成员方法，让这个类是线程安全的。那管程是如何管理的呢？</li>
<li>MESA 模型<br>有三种管程模型：Hasen、Hoare、MESA模型。Java用的是MESA。</li>
</ol>
<p>在并发编程领域，有两大核心：互斥&同步。互斥即同一个时刻只允许一个线程访问共享资源；同步即线程之间如何通信、协作。这两大问题都是管程要解决的。</p>
<ul>
<li>互斥问题：</li>
</ul>
<p>管程的解决思路：将共享变量&对共享变量的操作统一封装起来。<br>如下图所示：<br><img src="../../images/monitor.png" alt="Alt text"><br>上图中管程X将共享变量queue这个队列和相关操作入队 enq()、出队 deq() 都封装起来；线程A和线程B如果想访问共享变量queue,只能通过调用管程提供的 enq(),deq()方法来实现；而这俩方法可以保证互斥，只允许一个线程进入管程。前面的互斥锁用法原型也是它。</p>
<ul>
<li>同步问题：</li>
</ul>
<p><img src="../../images/mesamonitor.png" alt="Alt text"><br>在管程模型中，共享变量及其操作都是被封装起来的，图中最外层就是代表封装的意思。入口等待队列只允许一个线程进入。条件变量A,B都有自己的等待队列。这里的条件变量和等待队列就是用来解决同步问题的。</p>
<p>e.g. 线程a要执行出队操作，但队列不能为空，所以就进入其等待队列等，该操作由wait()实现。等有b线程入队了，此时b就通知a，该操作由notify()完成，当a得到消息后即出队。出对后重新进入到入口等待队列。(类似验血完重新回来找大夫分诊)</p>
<ul>
<li>阻塞队列的代码实现<br>该队列有两个入队出队操作，都是先获取互斥锁，类比管程模型的入口：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">public class BlockedQueue<T>{</span><br><span class="line">  final Lock lock = </span><br><span class="line">    new ReentrantLock();</span><br><span class="line">    // 条件变量：队列不满</span><br><span class="line">    final Condition notFull = </span><br><span class="line">      lock.newCondition();</span><br><span class="line">      // 条件变量：队列不变</span><br><span class="line">      final Condition notEmpty =</span><br><span class="line">        lock.newCondion();</span><br><span class="line"></span><br><span class="line">        // 入队</span><br><span class="line">        void enq(T x) {</span><br><span class="line">          lock.lock();</span><br><span class="line">          try {</span><br><span class="line">            <span class="keyword">while</span> (队列已满){</span><br><span class="line">              //等待队列不满</span><br><span class="line">              notFull.await();</span><br><span class="line">            }</span><br><span class="line">            // 省略入队操作。。。</span><br><span class="line">            // 入队后，通知出队</span><br><span class="line">            notEmpty.signal();</span><br><span class="line">          }finally {</span><br><span class="line">            lock.unlock();</span><br><span class="line">          }</span><br><span class="line">        }</span><br><span class="line">        // 出队</span><br><span class="line">        void <span class="function"><span class="title">deq</span></span>(){</span><br><span class="line">          lock.lock();</span><br><span class="line">          try {</span><br><span class="line">            <span class="keyword">while</span> (队列已空){</span><br><span class="line">              //等待队列不空</span><br><span class="line">              notEmpty.await();</span><br><span class="line">            }</span><br><span class="line">            // 省略出队操作...</span><br><span class="line">            // 出队后，通知入队</span><br><span class="line">            notFull.signal();</span><br><span class="line">          }finally {</span><br><span class="line">            lock.unlock();</span><br><span class="line">          }</span><br><span class="line">        }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
在以上代码中，用了Java并发包里的Lock和Condition。在这里await() = wait();signal()=notify()。</li>
</ul>
<ol start="3">
<li>wait() 的正确姿势<br>对于MESA 管程来说，需要在while循环里调用wait()。<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(条件不满足){</span><br><span class="line">  <span class="built_in">wait</span>();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
Hasen 模型、Hoare 模型和 MESA 模型的一个核心区别就是当条件满足后，如何通知相关线程。管程要求同一时刻只允许一个线程执行，那当线程 T2 的操作使线程 T1 等待的条件满足时，T1 和 T2 究竟谁可以执行呢？</li>
</ol>
<ul>
<li><p>Hasen 模型里面，要求 notify() 放在代码的最后，这样 T2 通知完 T1 后，T2 就结束了，然后 T1 再执行，这样就能保证同一时刻只有一个线程执行。</p>
</li>
<li><p>Hoare 模型里面，T2 通知完 T1 后，T2 阻塞，T1 马上执行；等 T1 执行完，再唤醒 T2，也能保证同一时刻只有一个线程执行。但是相比 Hasen 模型，T2 多了一次阻塞唤醒操作。</p>
</li>
<li><p>MESA 管程里面，T2 通知完 T1 后，T2 还是会接着执行，T1 并不立即执行，仅仅是从条件变量的等待队列进到入口等待队列里面。这样做的好处是 notify() 不用放到代码的最后，T2 也没有多余的阻塞唤醒操作。但是也有个副作用，就是当 T1 再次执行的时候，可能曾经满足的条件，现在已经不满足了，所以需要以循环方式检验条件变量。</p>
</li>
</ul>
<ol start="4">
<li>notify() 何时用<br>还有一个需要注意的地方，就是 notify() 和 notifyAll() 的使用，除非经过深思熟虑，否则尽量使用 notifyAll()。那什么时候可以使用 notify() 呢？需要满足以下三个条件：<br>(1) 所有等待线程拥有相同的等待条件；<br>(2) 所有等待线程被唤醒后，执行相同的操作；<br>(3) 只需要唤醒一个线程。</li>
</ol>
<p>比如上面阻塞队列的例子中，对于“队列不满”这个条件变量，其阻塞队列里的线程都是在等待“队列不满”这个条件，反映在代码里就是下面这 3 行代码。对所有等待线程来说，都是执行这 3 行代码，重点是 while 里面的等待条件是完全相同的。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (队列已满){</span><br><span class="line">  //等待队列不满</span><br><span class="line">  notFull.await();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>所有等待线程被唤醒后执行的操作也是相同的，都是：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">//省略入队操作。。。</span><br><span class="line">//入队后，通知可出队</span><br><span class="line">notEmpty.signal();</span><br></pre></td></tr></tbody></table></figure>
<p>同时也满足第3条，只需要唤醒一个线程。所以上面阻塞队列的代码，使用signal() 是可以的。</p>
<ol start="5">
<li>思考<br>wait()方法，在Hasen模型和Hoare模型里都是没有参数的，而在MESA模型里面，增加了超时参数，这个参数有必要加吗？</li>
</ol>
<ul>
<li><p>有必要 hasen是执行完再去唤醒另外一个线程，能够保证线程的执行；Hoare是中断当前线程唤醒另外一个线程，执行完再去唤醒，也能够保证完成。而MESA是进入等待队列，不一定有机会能够执行。</p>
</li>
<li><p>管程MESA模型，刚开始一直在想线程T1执行出队是什么意思？到底是哪个队列，是入口等待队列，还是条件等待队列，后来理解了都不是。这个队列应该理解为JDK里面的阻塞队列，里面存在的是共享数据，线程T1,T2分别去操作里面的共享数据，执行数据的入队，出队操作，当然这些操作是阻塞操作。当线程T1对阻塞队列执行数据出队操作时，进入管程，发现阻塞队列为空，此时线程T1进入阻塞队列不为空这个条件的条件等待队列，此时，其他线程还是可以进入管程的，比如T2进来了，对阻塞队列执行数据插入操作，这时就会致使线程T1从条件等待队列出来，进入入口等待队列，准备再一次进入管程……至于wait方法的参数，还是有必要的，因为可能线程需要的条件可能一直无法满足！</p>
</li>
<li><p>在java中，每个加锁的对象都绑定着一个管程（监视器）。线程访问加锁对象，就是去拥有一个监视器的过程。如一个病人去门诊室看医生，医生是共享资源，门锁锁定医生，病人去看医生，就是访问医生这个共享资源，门诊室其实是监视器（管程）。所有线程访问共享资源，都需要先拥有监视器。就像所有病人看病都需要先拥有进入门诊室的资格。监视器至少有两个等待队列。一个是进入监视器的等待队列一个是条件变量对应的等待队列。后者可以有多个。就像一个病人进入门诊室诊断后，需要去验血，那么它需要去抽血室排队等待。另外一个病人心脏不舒服，需要去拍胸片，去拍摄室等待。监视器要求的条件满足后，位于条件变量下等待的线程需要重新在门诊室门外排队，等待进入监视器。就像抽血的那位，抽完后，拿到了化验单，然后，重新回到门诊室等待，然后进入看病，然后退出，医生通知下一位进入。</p>
</li>
</ul>
<p>总结起来就是，管程就是一个对象监视器。任何线程想要访问该资源，就要排队进入监控范围。进入之后，接受检查，不符合条件，则要继续等待，直到被通知，然后继续进入监视器。</p>
<ul>
<li>java用两种方式实现了管程：(1) synchronized+wait、notify、notifyAll;(2) lock+内部的condition，第一种只支持一个条件变量，即wait，调用wait时会将其加到等待队列中，被notify时，会随机通知一个线程加到获取锁的等待队列中;第二种condition支持中断和增加了时间的等待，lock需要自己进行加锁解锁，更加灵活，两个都是可重入锁，但是lock支持公平和非公平锁，synchronized支持非公平锁。</li>
</ul>
<hr>
<h3 id="09-Java线程"><a href="#09-Java线程" class="headerlink" title="09 Java线程"></a>09 Java线程</h3><ol>
<li>线程的生命周期<br>(1) 通用的线程生命周期<br><img src="../../images/threadlife.png" alt="Alt text"></li>
</ol>
<ul>
<li><p>初始状态：指的是线程已经被创建(编程语言层面)，但仍不允许分配CPU执行(操作系统层面真正的线程还未被创建)。</p>
</li>
<li><p>可运行状态：线程可以分配CPU执行（即真正的操作系统线程已经被成功创建）。</p>
</li>
<li><p>运行状态：处于可运行状态的线程被分配了CPU。</p>
</li>
<li><p>休眠状态：运行状态的线程如果调用一个阻塞的API（例如以阻塞方式读文件）或者等待某个事件（例如条件变量），那么线程的状态就会转换到休眠状态，同时释放CPU使用权，休眠状态的线程没有机会获得CPU使用权。当等待事件出现线程就会从休眠状态转到可运行状态。</p>
</li>
<li><p>终止状态：线程执行完或抛异常就会进入终止，此时该线程的生命周期结束。</p>
</li>
<li><ul>
<li>五种状态在不同语言里会有简化合并。Java语言里则把可运行状态和运行状态合并了，这俩状态在操作系统层面有用，而JVM层面不关心这俩状态，因为JVM把线程调度交给操作系统处理了。此外，Java里还细化了休眠状态。</li>
</ul>
</li>
</ul>
<p>(2) Java中线程的生命周期</p>
<ul>
<li>Java线程有6种：NEW（初始化状态）、 RUNNABLE（可运行/运行状态）、 BLOCKED（阻塞状态）、 WAITING（无限时等待）、 TIMED_WAITING（有限时等待）、TERMINATED（终止状态）。<br><img src="../../images/javathreadlife.png" alt="Alt text"></li>
</ul>
<p>(3) RUNNABLE 与 BLOCKED 的状态转换</p>
<ul>
<li>只有一种场景，即线程等待synchronized的隐式锁。因为synchronized是单线程。当等待的线程获得synchronized隐式锁时就会从BLOCKED转换到RUNNABLE状态。</li>
<li>线程调用阻塞式 API 时，是否会转换到 BLOCKED 状态呢？在操作系统层面，线程是会转换到休眠状态的，但是在 JVM 层面，Java 线程的状态不会发生变化，也就是说 Java 线程的状态会依然保持 RUNNABLE 状态。JVM 层面并不关心操作系统调度相关的状态，因为在 JVM 看来，等待 CPU 使用权（操作系统层面此时处于可执行状态）与等待 I/O（操作系统层面此时处于休眠状态）没有区别，都是在等待某个资源，所以都归入了 RUNNABLE 状态。而我们平时所谓的 Java 在调用阻塞式 API 时，线程会阻塞，指的是操作系统线程的状态，并不是 Java 线程的状态。</li>
</ul>
<p>(4) RUNNABLE 与 WAITING 的状态转换</p>
<ul>
<li>有三种场景会触发：</li>
</ul>
<p>（一）获得synchronized隐式锁的线程，调用无参数的Object.wait()方法。</p>
<p>（二）调用无参数的Thread.join()方法。其中join()是一种线程同步方法，</p>
<p>（三） 调用LockSupport.park()方法。调用此方法时当前线程阻塞，从RUNNABLE到WAITING。调用LockSupport.unpark(Thread thread)可唤醒目标线程，目标线程从等待到可运行。</p>
<p>(5) RUNNABLE 与 TIMED_WAITING 的状态转换</p>
<ul>
<li>与waiting相对，仅仅是触发条件多了超时参数。如调用带超时参数的Thread.sleep(long millis)\Object.wait(long timeout)\Thread.join(long millis)\LockSupport.parkNanos(Object blocker, long deadline)\LockSupport.parkUntil(long deadline)等等。</li>
</ul>
<p>(6) 从NEW 到 RUNNABLE 状态<br>Java刚创建出来的Thread对象就是NEW状态，而创建Thread对象主要有两种方法。</p>
<ul>
<li><p>（一）继承Thread对象，重写run()方法：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">// 自定义线程对象</span><br><span class="line">class MyThread extends Thread {</span><br><span class="line">  public void <span class="function"><span class="title">run</span></span>() {</span><br><span class="line">    // 线程需要执行的代码</span><br><span class="line">    。。。</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line">// 创建线程对象</span><br><span class="line">MyThread myThread = new MyThread();</span><br></pre></td></tr></tbody></table></figure>
</li>
<li><p>(二) 实现Runnable接口，重写run()方法，并将该实现类作为创建Thread对象的参数：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">// 实现Runnable接口</span><br><span class="line">class Runner implements Runnable {</span><br><span class="line">  @Override void <span class="function"><span class="title">run</span></span>() {</span><br><span class="line">    // 线程需要执行的代码</span><br><span class="line">    。。。。</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line">// 创建线程对象</span><br><span class="line">Thread thread = new Thread(new Runner());</span><br></pre></td></tr></tbody></table></figure>
<p>NEW状态的线程，不会被操作系统调度，因此不会执行。Java线程要执行，就必须转换到RUNNABLE状态。从NEW状态转换到RUNNABLE状态很简单，只要调用线程对象的start()方法就可以了，如下：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">MyThread myThread = new MyThread();</span><br><span class="line">// 从NEW状态转换到RUNNABLE状态</span><br><span class="line">myThread.start();</span><br></pre></td></tr></tbody></table></figure>
<p>(7) 从 RUNNABLE 到 TERMINATED 状态<br>线程执行完run()后或者抛异常时，就会自动转到TERMINATED状态。有时需要强制中断run()方法的执行，此时调用interrupt()方法。interrupt()方法通知线程，线程有机会执行一些后续操作，同时也可以无视这个通知。</p>
</li>
<li><p>被 interrupt 的线程，是怎么收到通知的呢？一种是异常，另一种是主动检测。</p>
</li>
</ul>
<p>当线程 A 处于 WAITING、TIMED_WAITING 状态时，如果其他线程调用线程 A 的 interrupt() 方法，会使线程 A 返回到 RUNNABLE 状态，同时线程 A 的代码会触发 InterruptedException 异常。上面我们提到转换到 WAITING、TIMED_WAITING 状态的触发条件，都是调用了类似 wait()、join()、sleep() 这样的方法，我们看这些方法的签名，发现都会 throws InterruptedException 这个异常。</p>
<p>这个异常的触发条件就是：其他线程调用了该线程的 interrupt() 方法。当线程 A 处于 RUNNABLE 状态时，并且阻塞在 java.nio.channels.InterruptibleChannel 上时，如果其他线程调用线程 A 的 interrupt() 方法，线程 A 会触发 java.nio.channels.ClosedByInterruptException 这个异常；而阻塞在 java.nio.channels.Selector 上时，如果其他线程调用线程 A 的 interrupt() 方法，线程 A 的 java.nio.channels.Selector 会立即返回。</p>
<p>上面这两种情况属于被中断的线程通过异常的方式获得了通知。还有一种是主动检测，如果线程处于 RUNNABLE 状态，并且没有阻塞在某个 I/O 操作上，例如中断计算圆周率的线程 A，这时就得依赖线程 A 主动检测中断状态了。如果其他线程调用线程 A 的 interrupt() 方法，那么线程 A 可以通过 isInterrupted() 方法，检测是不是自己被中断了。</p>
<p>(8) 总结</p>
<ul>
<li>多线程程序很难调试，出了bug基本只能靠日志，靠线程dump跟踪问题，分析线程dump的一个基本功就是分析线程状态，大部分死锁、饥饿、活锁问题都需要跟踪分析线程的状态。</li>
</ul>
<p>可以通过jstack命令或者Java visualVM等可视化工具将JVM所有线程栈信息导出，完整的线程栈信息不仅包括线程的当前状态、调用栈、还包括了锁的信息。如下：<br><img src="../../images/deadlock.png" alt="Alt text"></p>
<ul>
<li>以上导出的线程明确告知了死锁，并将死锁的调用栈信息也显示出来了。</li>
</ul>
<p>(9) 思考</p>
<ul>
<li><p>这块代码意为当前线程被中断后退出 while(true),是否正确？</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">Thread th = Thread.currentThread();</span><br><span class="line"><span class="keyword">while</span>(<span class="literal">true</span>) {</span><br><span class="line">  <span class="keyword">if</span>(th.isInterrupted()) {</span><br><span class="line">    <span class="built_in">break</span>;</span><br><span class="line">  }</span><br><span class="line">  // 省略业务代码无数</span><br><span class="line">  try {</span><br><span class="line">    Thread.sleep(100);</span><br><span class="line">  }catch (InterruptedException e){</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
</li>
<li><p>当发起中断后，Thread.sleep(100) 会抛出InterruptedException异常，而抛出这个异常会清除当前线程的中断标识，导致th.isInterrupted()一直返回false。InterruptedException - if any thread has interrupted the current thread. The interrupted status of the current thread is cleared when this exception is thrown.</p>
</li>
<li><p>所以需要加中断标识：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">...</span><br><span class="line">catch (InterruptedException e){</span><br><span class="line">  Thread.currentThread().interrupt();</span><br><span class="line">  e.printStackTrace();</span><br><span class="line">}</span><br><span class="line">...</span><br></pre></td></tr></tbody></table></figure>
</li>
</ul>
<hr>
<ol start="2">
<li>创建多少线程合适</li>
</ol>
<ul>
<li><p>性能指标：降低延迟；提高吞吐量</p>
</li>
<li><p>方法：优化算法；将硬件性能发挥到极致。<br>提升性能本质：提升I/O的利用率和CPU的利用率。</p>
</li>
<li><p>这里需要解决CPU和I/O设备综合利用率的问题——多线程给。</p>
</li>
<li><p>如果程序只有CPU计算，没有I/O操作的话，多线程会使性能更差，因为增加了线程切换的成本。但多核时代即使纯计算程序也可以利用多线程来提升性能，因为这可以降低响应时间。</p>
</li>
<li><p>下面分两种情形来说明计算最佳线程数的方法：</p>
</li>
<li><p>(一) 对于CPU密集型计算</p>
</li>
</ul>
<p>多线程对于CPU密集型计算,其本质是提升多核CPU的利用率，所以对于4核CPU理论上创建4个线程即可，再多创建只会增加线程切换成本。所以在这种情形下：线程数 = CPU 核数。但在工程上一般为CPU + 1，保证CPU利用率。</p>
<ul>
<li>(二) 对于I/O密集型计算</li>
</ul>
<p>如果CPU计算和I/O操作的耗时是1:2,三个线程合适，如下图,这样I/O和CPU利用率都是100%。：<br><img src="../../images/IODENSE.png" alt="Alt text"><br>由此得到公式:<br>单核：<br>最佳线程数 = 1 + (I/O / CPU 耗时)<br>多核：前面乘以核数即可。</p>
<ol start="3">
<li>为什么局部变量是线程安全的<br>e.g.<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">// 返回斐波那契数列</span><br><span class="line">int[] fibonacci(int n) {</span><br><span class="line">  // 创建结果数组</span><br><span class="line">  int[] r = new int[n];</span><br><span class="line">  //初始化第一、第二个数</span><br><span class="line">  r[0] = r[1] = 1; // ①</span><br><span class="line">  // 计算2...n</span><br><span class="line">  <span class="keyword">for</span>(int i = 2; i < n; i++) {</span><br><span class="line">    r[i] = r[i-2] + r[i-1];</span><br><span class="line">  }</span><br><span class="line">  <span class="built_in">return</span> r;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
</ol>
<ul>
<li>局部变量并不存在数据竞争<br>原因：从编译器实现者的角度思考方法到指令的转换<h4 id="（一）方法是如何被执行的"><a href="#（一）方法是如何被执行的" class="headerlink" title="（一）方法是如何被执行的"></a>（一）方法是如何被执行的</h4>调用Fibonacci()方法时，CPU要先找到该方法的地址，然后跳到该方法去执行，执行完再返回方法下一条语句的地址，再执行。参考下图：<br><img src="../../images/Fibonacci.png" alt="Alt text"><br>CPU通过它的堆栈寄存器找调用方法的参数和地址。CPU是一种栈结构，先入后出（弹夹）。因为这个栈是和方法调用相关的，所以称为调用栈。<br><img src="../../images/stack.png" alt="Alt text"><br>上图所示A->B->C，每个方法再调用栈里都有自己的独立空间————栈帧，每个战阵都有对应方法需要的参数和返回地址。当调用方法时，会创建新的栈帧，并压入调用栈；当方法返回时，对应的栈帧就会被自动弹出。所以，栈帧和方法是同生共死的。</li>
</ul>
<p>利用栈结构来支持方法调用这个方案非常普遍，以至于CPU内置了栈寄存器。各种编程语言都是靠栈结构解决的。</p>
<h4 id="（二）局部变量存哪里"><a href="#（二）局部变量存哪里" class="headerlink" title="（二）局部变量存哪里"></a>（二）局部变量存哪里</h4><p><img src="../../images/localvariable.png" alt="Alt text"><br>局部变量的作用域是方法内部，也就是说方法执行完局部变量就没用了，局部变量和方法同生共死。如上，调用栈的栈帧就是和方法同生共死的，所以局部变量放到调用栈里那儿是相当合理。所以调用栈结构就如上图所示。<br>学Java时，new出来的对象再堆里，局部变量在栈里。就是因为局部变量和方法时同生共死的，一个变量如果想跨越方法的边界，就必须创建在栈里。</p>
<h4 id="（三）调用栈和线程"><a href="#（三）调用栈和线程" class="headerlink" title="（三）调用栈和线程"></a>（三）调用栈和线程</h4><p><img src="../../images/threadstack.png" alt="Alt text"></p>
<h4 id="（四）线程封闭"><a href="#（四）线程封闭" class="headerlink" title="（四）线程封闭"></a>（四）线程封闭</h4><p>方法里的局部变量因为不会和其他线程共享，所以也没有并发问题，这叫线程封闭。例子很多，如：从数据库连接池里获取的连接connection，在JDBC规范里并没有要求这个connection必须是线程安全的。数据库连接池通过线程封闭技术，保证一个connection一旦被一个线程获取之后，在这个线程关闭Connection之前的这段时间里，不会再分配给其他线程，从而保证了connection不会有并发问题。</p>
<h4 id="（五）思考"><a href="#（五）思考" class="headerlink" title="（五）思考"></a>（五）思考</h4><p>递归调用太深可能导致栈溢出，为什么？解决方案？</p>
<p>栈溢出原因：<br>因为每调用一个方法就会在栈上创建一个栈帧，方法调用结束后就会弹出该栈帧，而栈的大小不是无限的，所以递归调用次数过多的话就会导致栈溢出。而递归调用的特点是每递归一次，就要创建一个新的栈帧，而且还要保留之前的环境（栈帧），直到遇到结束条件。所以递归调用一定要明确好结束条件，不要出现死循环，而且要避免栈太深。<br>解决方法：</p>
<ol>
<li>简单粗暴，不要使用递归，使用循环替代。缺点：代码逻辑不够清晰；</li>
<li>限制递归次数；</li>
<li>使用尾递归，尾递归是指在方法返回时只调用自己本身，且不能包含表达式。编译器或解释器会把尾递归做优化，使递归方法不论调用多少次，都只占用一个栈帧，所以不会出现栈溢出。然鹅，Java没有尾递归优化。</li>
</ol>
<hr>
<h3 id="10-如何用面向对象思想写好并发程序"><a href="#10-如何用面向对象思想写好并发程序" class="headerlink" title="10 如何用面向对象思想写好并发程序"></a>10 如何用面向对象思想写好并发程序</h3><h4 id="一、封装共享变量"><a href="#一、封装共享变量" class="headerlink" title="一、封装共享变量"></a>一、封装共享变量</h4><p>面向对象的重要思想就是封装，即将属性和实现细节封装在对象内部，外界对象只能通过目标对象提供的公共方法来间接访问这些内部属性。</p>
<p>利用面向对象写并发程序，其实就是：将共享变量作为对象属性封装在内部，对所有公共方法指定并发访问策略。<br>代码：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">public class Counter {</span><br><span class="line">  private long value;</span><br><span class="line">  synchronized long <span class="function"><span class="title">get</span></span>() {</span><br><span class="line">    <span class="built_in">return</span> value;</span><br><span class="line">  }</span><br><span class="line">  synchronized long <span class="function"><span class="title">addOne</span></span>() {</span><br><span class="line">    <span class="built_in">return</span> ++value;</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>以上计数器程序共享变量只有一个：value。把它作为Counter类的属性，并且将两个公共方法get(),addOne()声明为同步方法，这样Counter类就是一个线程安全的类了。</p>
<ul>
<li>很多共享变量的值是不变的，例如信用卡卡号、姓名、身份证。。。对于不会发生变化的共享变量，用final修饰。</li>
</ul>
<h4 id="二、识别共享变量间的约束条件"><a href="#二、识别共享变量间的约束条件" class="headerlink" title="二、识别共享变量间的约束条件"></a>二、识别共享变量间的约束条件</h4><p>因为约束条件决定了并发访问策略。如库存管理里面酉哥合理库存的概念，库存量不能太高，也不能太低，有一个上限和下限。如下类SafeWM中，变量upper,lower用了AtomicLong这个原子类，原子类是线程安全的，所以这俩成员变量的set方法就不需要同步了。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">public class SafeWM {</span><br><span class="line">  // 库存上限</span><br><span class="line">  private final AtomicLong upper =</span><br><span class="line">        new AtomicLong(0);</span><br><span class="line">  // 库存下限</span><br><span class="line">  private final AtomicLong lower = </span><br><span class="line">        new AtomicLong(0);</span><br><span class="line">  // 设置库存上限</span><br><span class="line">  void setUpper(long v){</span><br><span class="line">    upper.set(v);</span><br><span class="line">  }</span><br><span class="line">  // 设置库存下限</span><br><span class="line">  void setLower(long v){</span><br><span class="line">    lower.set(v);</span><br><span class="line">  }</span><br><span class="line">  // 省略其他业务代码 </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>上面忽视了约束条件：库存下限要小于库存上限，所以改进：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">...</span><br><span class="line">// 设置库存上限</span><br><span class="line">void setUpper(long v){</span><br><span class="line">  // 检查参数合法性</span><br><span class="line">  <span class="keyword">if</span> (v < lower.get()) {</span><br><span class="line">    throw new IllegalArgumentException();</span><br><span class="line">  }</span><br><span class="line">  lower.set(v);</span><br><span class="line">}</span><br><span class="line">  // 设置下限</span><br><span class="line">  void setLower(long v) {</span><br><span class="line">    //检查参数合法性</span><br><span class="line">    <span class="keyword">if</span> (v > upper.get()) {</span><br><span class="line">      throw new IllegalArgumentException();</span><br><span class="line">    }</span><br><span class="line">    lower.set(v);</span><br><span class="line">  }</span><br><span class="line">  // 省略其他业务代码</span><br><span class="line">。。。</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>假设库存的下限和上限分别是（2，10），线程A.setUpper(5),B.setLower(7),此时AB线程同时执行，则A可以通过参数校验，因为下限还未被B设置，还是2；当然同理B也能通过参数校验。当AB同时通过参数校验后，库存的下限上限就设置成了（7，5）了。</li>
</ul>
<h4 id="三、制定并发访问策略"><a href="#三、制定并发访问策略" class="headerlink" title="三、制定并发访问策略"></a>三、制定并发访问策略</h4><ol>
<li><p>避免共享：主要利于线程本地存储以及为每个任务分配独立的线程。</p>
</li>
<li><p>不变模式：在Java领域应用很少，其他领域多：Actor模式、CSP模式、函数式编程的基础都是不变模式。</p>
</li>
<li><p>管程及其他同步工具： Java领域的万能解决方案。但对于其它特定场景使用JAVA并发包提供的读写锁、并发容器等同步工具会更好。</p>
</li>
</ol>
<h4 id="宏观原则"><a href="#宏观原则" class="headerlink" title="宏观原则"></a>宏观原则</h4><ol>
<li>优先使用成熟的工具类</li>
<li>迫不得已再使用低级的同步原语</li>
<li>避免过早优化</li>
</ol>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>对共享变量进行封装要避免“逸出”，即共享变量逃逸到对象外面，如02|Java内存模型：看Java如何解决可见性和有序性问题中的this“逸出”。</p>
<h4 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h4><p>修改SafeWM不满足库存下限小于库存上限这个约束条件</p>
<ul>
<li><p>setUpper(),setLower()都加上“synchronized”关键字。</p>
</li>
<li><p>将lower，upper两个变量封装到一个类中：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">public class Boundary{</span><br><span class="line">  private final lower;</span><br><span class="line">  private final upper;</span><br><span class="line"></span><br><span class="line">  public Boundary(long lower,long upper) {</span><br><span class="line">    <span class="keyword">if</span>(lower >= upper) {</span><br><span class="line">      // throw exception</span><br><span class="line">    }</span><br><span class="line">    this.lower = lower;</span><br><span class="line">    this.upper = upper;</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>移除SafeWM的setUpper()setLower()方法，增加setBoundary(Boundary boundary)方法。</p>
</li>
<li><p>不能用volatile，因为其只可保证内存可见性，不能保证原子性，还得枷锁保证互斥。</p>
</li>
</ul>
<p>*</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">···········</span><br><span class="line">volatile AtomicReference<Inventory> inventory = new AtomicReference<>();</span><br><span class="line"></span><br><span class="line">    static class Inventory {</span><br><span class="line">        private volatile long upper = 0;</span><br><span class="line"></span><br><span class="line">        private volatile long lower = 0;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    void setUpper(long v) {</span><br><span class="line">        long low;</span><br><span class="line">        Inventory oldObj;</span><br><span class="line">        Inventory newObj;</span><br><span class="line">        <span class="keyword">do</span> {</span><br><span class="line">            oldObj = inventory.get();</span><br><span class="line">            <span class="keyword">if</span> (v >= (low = oldObj.lower)) {</span><br><span class="line">                throw new IllegalArgumentException();</span><br><span class="line">            }</span><br><span class="line">            newObj = new Inventory();</span><br><span class="line">            newObj.lower = low;</span><br><span class="line">            newObj.upper = v;</span><br><span class="line"></span><br><span class="line">        } <span class="keyword">while</span> (inventory.compareAndSet(oldObj, newObj));</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    void setLower(long v) {</span><br><span class="line">        long upp;</span><br><span class="line">        Inventory oldObj;</span><br><span class="line">        Inventory newObj;</span><br><span class="line">        <span class="keyword">do</span> {</span><br><span class="line">            oldObj = inventory.get();</span><br><span class="line">            <span class="keyword">if</span> (v <= (upp = oldObj.upper)) {</span><br><span class="line">                throw new IllegalArgumentException();</span><br><span class="line">            }</span><br><span class="line">            newObj = new Inventory();</span><br><span class="line">            newObj.lower = v;</span><br><span class="line">            newObj.upper = upp;</span><br><span class="line"></span><br><span class="line">        } <span class="keyword">while</span> (inventory.compareAndSet(oldObj, newObj));</span><br><span class="line">    }</span><br></pre></td></tr></tbody></table></figure>

<hr>
<h3 id="11-理论基础模块答疑"><a href="#11-理论基础模块答疑" class="headerlink" title="11 理论基础模块答疑"></a>11 理论基础模块答疑</h3><h4 id="1-第一部分总结"><a href="#1-第一部分总结" class="headerlink" title="1 第一部分总结"></a>1 第一部分总结</h4><p><img src="../../images/firstpart.png" alt="Alt text"></p>
<h4 id="2-用锁的最佳实践"><a href="#2-用锁的最佳实践" class="headerlink" title="2 用锁的最佳实践"></a>2 用锁的最佳实践</h4><p>一个受保护资源与锁的关联关系应为N:1<br>Integer String类型的对象不适合做锁，因其对象类型再JVM里面是可能被重用的。所以锁是私有的，不可变的，不可重用的。</p>
<h4 id="3-竟态条件需要格外关注"><a href="#3-竟态条件需要格外关注" class="headerlink" title="3 竟态条件需要格外关注"></a>3 竟态条件需要格外关注</h4><p>在07|安全性活跃性以及性能问题中的思考题是一种典型的竟态条件问题，如下：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">void addIfNotExist(Vector v,</span><br><span class="line">Object o) {</span><br><span class="line">  <span class="keyword">if</span>(!v.contains(o)) {</span><br><span class="line">    v.add(o);</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>以上代码里contains和add都是线程安全的，但合在一起就不是了，解决方法参考12讲中的将共享变量v封装在对象内部，然后控制并发访问路径，这样就能阻止对Vector v变量的滥用：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">class SafeVector{</span><br><span class="line">  private Vector v;</span><br><span class="line">  //所有公共方法增加同步控制</span><br><span class="line">  synchronized</span><br><span class="line">  void addIfNotExist(Object o){</span><br><span class="line">    <span class="keyword">if</span>(!v.contains(o)){</span><br><span class="line">      v.add(o);</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h4 id="4-方法调用是先计算参数"><a href="#4-方法调用是先计算参数" class="headerlink" title="4 方法调用是先计算参数"></a>4 方法调用是先计算参数</h4><ul>
<li>方法的调用，是先计算参数，然后将参数压入调用栈之后才会执行方法体，方法调用见11章。<h4 id="5-InterruptedException-异常处理需小心"><a href="#5-InterruptedException-异常处理需小心" class="headerlink" title="5 InterruptedException 异常处理需小心"></a>5 InterruptedException 异常处理需小心</h4>在9讲中，在调用对象的wait()方法或者线程的sleep()方法时，需要捕获并处理InterruptedException异常，在如下例子中本意是通过isInterrrupted()检查线程是否被中断，如果中断就退出while循环。当其他线程通过调用th.interrupted().来中断th线程时，会设置th线程的中断标志位，从而使th.isInterrupted()返回true，这样就能退出while循环：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">Thread th = Thread.currentThread();</span><br><span class="line"><span class="keyword">while</span>(<span class="literal">true</span>){</span><br><span class="line">  <span class="keyword">if</span>(th.isInterrupted()){</span><br><span class="line">    <span class="built_in">break</span>;</span><br><span class="line">  }</span><br><span class="line">  // 省略业务代码</span><br><span class="line">  try {</span><br><span class="line">    Thread.sleep(100);</span><br><span class="line">  } catch (InterruptedException e){</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
但实际中断th线程时大概率会触发InterruptedException异常，同时JVM会同时把线程的中断标志位清除，所以这个时候th.isInterrupted()返回的是false。所以：<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">...</span><br><span class="line">//重新中断标志位</span><br><span class="line">th.interrupt();</span><br><span class="line">...</span><br></pre></td></tr></tbody></table></figure>
<h4 id="6-理论值和经验值"><a href="#6-理论值和经验值" class="headerlink" title="6 理论值和经验值"></a>6 理论值和经验值</h4>实际中，不同的I/O模型对最佳线程数的影响非常大，例如Nginx用的是非阻塞I/O，采用的是多进程单线程结构，Nginx本来是一个I/O密集型系统，但是最佳进程数设置的却是CPU核数，完全参考了CPU密集型的算法。<h4 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h4>阅读材料：Java安全编码标准<br><a href="https://wiki.sei.cmu.edu/confluence/display/java/2+Rules" target="_blank" rel="noopener">https://wiki.sei.cmu.edu/confluence/display/java/2+Rules</a></li>
</ul>
</body></html>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>geekbang</tag>
        <tag>concurrent</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/01/26/hello-world/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
</body></html>]]></content>
  </entry>
  <entry>
    <title>Golang MVC structure</title>
    <url>/2020/01/15/Golang%20MVC%20Project/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h3 id="This-MVC-doesn’t-use-any-golang-web-framework"><a href="#This-MVC-doesn’t-use-any-golang-web-framework" class="headerlink" title="This MVC doesn’t use any golang web framework"></a>This MVC doesn’t use any golang web framework</h3><p>   Use http for client and server communication, MUX for routing</p>
<ol>
<li>structure:<br>in the G:/go-work/src/mvc-example:</li>
</ol>
<ul>
<li>app/controllers<br>home.go</li>
<li>app/models<br>types.go</li>
<li>app/route<br>route.go</li>
<li>app/helpers</li>
<li>config/.env</li>
<li>app/<br>main.go</li>
</ul>
<ol start="2">
<li><p>modify & create:<br>comment on app/controllers like this:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">// <span class="string">"github.com/gorilla/mux"</span></span><br><span class="line">// <span class="string">"mvc_example/app/models"</span></span><br><span class="line">// <span class="string">"encoding/json"</span></span><br><span class="line">// <span class="built_in">log</span> <span class="string">"github.com/sirupsen/logrus"</span></span><br></pre></td></tr></tbody></table></figure>
<p>create .env file that have key value pair of environment variable, and access that file using joho/godotenv package.add below key/value pair for test:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">site : <span class="string">"restapiexample.com"</span></span><br></pre></td></tr></tbody></table></figure>
<p>Load the .env file into main function and access env variable using key ‘site’, that will return ‘restapiexample.com’.</p>
</li>
<li><p>codes</p>
</li>
</ol>
<ul>
<li>main:<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">	<span class="string">"github.com/joho/godotenv"</span></span><br><span class="line">	<span class="built_in">log</span> <span class="string">"github.com/sirupsen/logrus"</span></span><br><span class="line">	<span class="string">"os"</span></span><br><span class="line">	<span class="string">"mvc_example/app/route"</span></span><br><span class="line">	<span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">func <span class="function"><span class="title">main</span></span>() {</span><br><span class="line">	log.Info(<span class="string">"Application has been started on : 8080"</span>)</span><br><span class="line">	err := godotenv.Load(os.ExpandEnv(<span class="string">"<span class="variable">$GOPATH</span>/src/mvc_example/app/config/.env"</span>))</span><br><span class="line">	<span class="keyword">if</span> err != nil {</span><br><span class="line">		log.Fatal(<span class="string">"Error loading .env file"</span>)</span><br><span class="line">	}</span><br><span class="line"></span><br><span class="line">	key := os.Getenv(<span class="string">"Site"</span>)</span><br><span class="line">	log.Info(key)</span><br><span class="line"></span><br><span class="line">	//creating instance of mux</span><br><span class="line">	routes := route.GetRoutes()</span><br><span class="line">	http.Handle(<span class="string">"/"</span>, routes)</span><br><span class="line">	log.Fatal(http.ListenAndServe(<span class="string">":8080"</span>, nil))</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
<li>controllers/home.go:<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">package controllers</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">	<span class="string">"fmt"</span></span><br><span class="line">	<span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">func Hello(w http.ResponseWriter, r *http.Request) {</span><br><span class="line">	fmt.Println(<span class="string">"Hello, I am home controller!"</span>)</span><br><span class="line">	w.WriteHeader(http.StatusOK)</span><br><span class="line">	fmt.Fprintf(w, <span class="string">"Hello, I am home controller"</span>)</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
<li>models/types.go:<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">package models</span><br><span class="line"> </span><br><span class="line"><span class="built_in">type</span> User struct {</span><br><span class="line">   Data struct {</span><br><span class="line">      ID        int    `json:<span class="string">"id"</span>`</span><br><span class="line">      FirstName string `json:<span class="string">"first_name"</span>`</span><br><span class="line">      LastName  string `json:<span class="string">"last_name"</span>`</span><br><span class="line">      Avatar    string `json:<span class="string">"avatar"</span>`</span><br><span class="line">   } `json:<span class="string">"data"</span>`</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
<li>route/route.go<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">package route</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">	<span class="string">"github.com/gorilla/mux"</span></span><br><span class="line">	<span class="string">"mvc_example/app/controllers"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">func GetRoutes() *mux.Router {</span><br><span class="line">	routes := mux.NewRouter().StrictSlash(<span class="literal">false</span>)</span><br><span class="line"></span><br><span class="line">	routers := routes.PathPrefix(<span class="string">"/api/v1"</span>).Subrouter()</span><br><span class="line"></span><br><span class="line">	routers.HandleFunc(<span class="string">"/hello"</span>, controllers.Hello).Methods(<span class="string">"GET"</span>)</span><br><span class="line"></span><br><span class="line">	<span class="built_in">return</span> routers</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>


</li>
</ul>
<p>Access rest call using “<a href="http://localhost:8080/api/v1/hello"" target="_blank" rel="noopener">http://localhost:8080/api/v1/hello"</a></p>
</body></html>]]></content>
      <categories>
        <category>golang</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>web</tag>
        <tag>MVC</tag>
      </tags>
  </entry>
  <entry>
    <title>go get cmd</title>
    <url>/2020/01/13/go-get-cmd/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h3 id="go-get-cmd"><a href="#go-get-cmd" class="headerlink" title="go get cmd"></a>go get cmd</h3><ol>
<li><p>go get<br>下载导入路径指定的包及其依赖项，然后安装命名包，即执行go install命令。<br>用法：go get [-d] [-f] [-t] [-u] [-fix] [-insecure] [build flags] [packages]</p>
</li>
<li><p>标记名称                     描述</p>
</li>
</ol>
<p>-d    让命令程序只执行下载动作，而不执行安装动作。<br>-f    仅在使用-u标记时有效,该标记会让命令程序忽略掉对已下载代码包的导入路径的检查。如下载并安装的代码包所属的项目是Fork的。<br>-fix    让命令程序在下载代码包后先执行修正动作，而后再进行编译和安装。<br>-insecure    使用非安全scheme（如HTTP）去下载指定的代码包。如果用的代码仓库（如公司内部的Gitlab）没有HTTPS支持，可以添加此标记。<br>-t    让命令程序同时下载并安装指定的代码包中的测试源码文件中依赖的代码包。<br>-u    让命令利用网络来更新已有代码包及其依赖包。默认情况下，该命令只会从网络上下载本地不存在的代码包，而不会更新已有的代码包。<br>-v    打印出被构建的代码包的名字<br>-x    打印出用到的命令<br>3. go install<br>使用：go install [-i] [build flags] [packages]。<br>和go build命令比较相似，go build命令会编译包及其依赖，生成的文件存放在当前目录下。而且go build只对main包有效，其他包不起作用。而go install对于非main包会生成静态文件放在$GOPATH/pkg目录下，文件扩展名为a。如果为main包，则会在$GOPATH/bin下生成一个和给定包名相同的可执行二进制文件。</p>
<p>综上： go get命令会下载指定的包，并将下载的包进行编译，然后安装到特定目录。</p>
</body></html>]]></content>
      <categories>
        <category>golang</category>
      </categories>
      <tags>
        <tag>go get</tag>
        <tag>cmd</tag>
      </tags>
  </entry>
  <entry>
    <title>upgrade mysql</title>
    <url>/2020/01/08/mysqlupgrade/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h3 id="mysql-upgrade-from-5-6-to-8-0-18"><a href="#mysql-upgrade-from-5-6-to-8-0-18" class="headerlink" title="mysql upgrade(from 5.6 to 8.0.18)"></a>mysql upgrade(from 5.6 to 8.0.18)</h3><ol>
<li>find installation location</li>
</ol>
<ul>
<li>open cmd type mysql,and then<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">show variables like <span class="string">"%char%"</span>;</span><br><span class="line">+--------------------------+---------------------------------------------------------+</span><br><span class="line">| Variable_name            | Value                                                   |</span><br><span class="line">+--------------------------+---------------------------------------------------------+</span><br><span class="line">| character_set_client     | gbk                                                     |</span><br><span class="line">| character_set_connection | gbk                                                     |</span><br><span class="line">| character_set_database   | latin1                                                  |</span><br><span class="line">| character_set_filesystem | binary                                                  |</span><br><span class="line">| character_set_results    | gbk                                                     |</span><br><span class="line">| character_set_server     | latin1                                                  |</span><br><span class="line">| character_set_system     | utf8                                                    |</span><br><span class="line">| character_sets_dir       | D:\Program Files\MySQL\MySQL Server 5.6\share\charsets\ |</span><br><span class="line">+--------------------------+---------------------------------------------------------+</span><br></pre></td></tr></tbody></table></figure></li>
</ul>
<ol start="2">
<li>remove old version service<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">D:\Program Files\MySQL\MySQL Server 5.6\bin>mysqld --remove mysql</span><br><span class="line">Failed to remove the service because the service is running</span><br><span class="line">Stop the service and try again</span><br><span class="line">D:\Program Files\MySQL\MySQL Server 5.6\bin>net stop mysql</span><br><span class="line">MySQL 服务正在停止.</span><br><span class="line">MySQL 服务已成功停止。</span><br><span class="line">D:\Program Files\MySQL\MySQL Server 5.6\bin>mysqld --remove mysql</span><br><span class="line">Service successfully removed.</span><br></pre></td></tr></tbody></table></figure></li>
<li>install new<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">D:\Program Files\MySQL\MySQL Server 8.0.18\bin>mysqld --install mysql</span><br><span class="line">Service successfully installed.</span><br><span class="line">D:\Program Files\MySQL\MySQL Server 8.0.18\bin>mysqld --initialize-insecure</span><br><span class="line"><span class="comment"># then a new fold data is created</span></span><br><span class="line">D:\Program Files\MySQL\MySQL Server 8.0.18\bin>net start mysql</span><br><span class="line">mysql 服务正在启动 .</span><br><span class="line">mysql 服务已经启动成功。</span><br></pre></td></tr></tbody></table></figure></li>
<li>upgrade<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">D:\Program Files\MySQL\MySQL Server 8.0.18\bin>mysql_upgrade -uroot -p123</span><br><span class="line">mysql_upgrade: [Warning] Using a password on the <span class="built_in">command</span> line interface can be insecure.</span><br><span class="line">The mysql_upgrade client is now deprecated. The actions executed by the upgrade client are now <span class="keyword">done</span> by the server.</span><br><span class="line">To upgrade, please start the new MySQL binary with the older data directory. Repairing user tables is <span class="keyword">done</span> automatically. Restart is not required after upgrade.</span><br><span class="line">The upgrade process automatically starts on running a new MySQL binary with an older data directory. To avoid accidental upgrades, please use the --upgrade=NONE option with the MySQL binary. The option --upgrade=FORCE is also provided to run the server upgrade sequence on demand.</span><br><span class="line">It may be possible that the server upgrade fails due to a number of reasons. In that <span class="keyword">case</span>, the upgrade sequence will run again during the next MySQL server start. If the server upgrade fails repeatedly, the server can be started with the --upgrade=MINIMAL option to start the server without executing the upgrade sequence, thus allowing users to manually rectify the problem.</span><br><span class="line"></span><br><span class="line">D:\Program Files\MySQL\MySQL Server 8.0.18\bin>net stop mysql</span><br><span class="line">...</span><br><span class="line">D:\Program Files\MySQL\MySQL Server 8.0.18\bin>net start mysql</span><br><span class="line">...</span><br></pre></td></tr></tbody></table></figure></li>
<li>use<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">C:\Users\user>mysql -u root -p</span><br><span class="line">Enter password:</span><br><span class="line">Welcome to the MySQL monitor.  Commands end with ; or \g.</span><br><span class="line">Your MySQL connection id is 9</span><br><span class="line">Server version: 8.0.18 MySQL Community Server - GPL</span><br></pre></td></tr></tbody></table></figure></li>
<li>ERROR 1045 (28000): Access denied for user ‘ODBC’@’localhost’<br>this error pop up when type just “mysql”. That’s because ODBC is the default username under win system.</li>
</ol>
</body></html>]]></content>
      <categories>
        <category>sql</category>
      </categories>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>configure apache in docker while docker is in WSL</title>
    <url>/2020/01/03/WSL-Docker/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h3 id="Apache"><a href="#Apache" class="headerlink" title="Apache"></a>Apache</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ mkdir -p  mkdir -p  ~/apache/www ~/apache/logs ~/apache/conf</span><br><span class="line">$ <span class="built_in">cd</span> apache</span><br><span class="line">~/apache$ touch Dockerfile</span><br><span class="line">~/apache$ vi Dockerfile</span><br><span class="line">~/apache$ touch httpd-foreground</span><br><span class="line">~/apache$ vi httpd-foreground</span><br><span class="line">~/apache$ chmod +x httpd-foreground</span><br><span class="line">...service docker start</span><br><span class="line">...service docker status</span><br><span class="line">~/apache$ docker build -t httpdliuying .</span><br></pre></td></tr></tbody></table></figure>
<p>Then you’ll get this error: Cannot connect to the Docker daemon at unix:///var/run/docker.sock. Is the docker daemon running?</p>
<h3 id="SSH"><a href="#SSH" class="headerlink" title="SSH"></a>SSH</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"> sudo cp /etc/ssh/sshd_config /etc/ssh/sshd_config.bak</span><br><span class="line"> sudo vim /etc/ssh/sshd_config</span><br><span class="line"> sudo service ssh start <span class="comment"># excute it when you login Xftp 6</span></span><br><span class="line"> * Starting OpenBSD Secure Shell server sshd                                                                                                     /etc/ssh/sshd_config: line 12: Bad configuration option: service</span><br><span class="line"> /etc/ssh/sshd_config: line 13: Bad configuration option: service</span><br><span class="line"> /etc/ssh/sshd_config: terminating, 2 bad configuration options</span><br><span class="line"></span><br><span class="line"> [fail]</span><br><span class="line"><span class="comment"># sudo service ssh status</span></span><br><span class="line"> * sshd is not running</span><br></pre></td></tr></tbody></table></figure>
<p> Then, you should uninstall it & reinstall it:<br> </p><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment"># sudo apt-get purge openssh-server</span></span><br><span class="line"><span class="comment"># sudo apt-get install openssh-server</span></span><br><span class="line"><span class="comment"># sudo cp /etc/ssh/sshd_config /etc/ssh/sshd_config.bak</span></span><br><span class="line"><span class="comment"># sudo vim /etc/ssh/sshd_config</span></span><br><span class="line"><span class="comment"># sudo service ssh start</span></span><br><span class="line"> * Starting OpenBSD Secure Shell server sshd                                                                                                     [ OK ]</span><br><span class="line"><span class="comment"># sudo service ssh status</span></span><br><span class="line"> * sshd is running</span><br><span class="line"><span class="comment"># sudo systemctl enable ssh</span></span><br><span class="line">Synchronizing state of ssh.service with SysV service script with /lib/systemd/systemd-sysv-install.</span><br><span class="line">Executing: /lib/systemd/systemd-sysv-install <span class="built_in">enable</span> ssh</span><br><span class="line"> ...</span><br></pre></td></tr></tbody></table></figure><p></p>
</body></html>]]></content>
      <categories>
        <category>WSL</category>
      </categories>
      <tags>
        <tag>WSL</tag>
        <tag>SSH</tag>
        <tag>apache</tag>
      </tags>
  </entry>
  <entry>
    <title>create cli by cobra</title>
    <url>/2019/12/30/cobra/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><p><img src="../../images/cobra.png" alt="Alt text"></p>
<h3 id="install"><a href="#install" class="headerlink" title="install"></a>install</h3><p>cd to ../golang.org/x</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/golang/text</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/golang/sys</span><br></pre></td></tr></tbody></table></figure>
<p>download github.com/spf13/cobra/cobra<br>cd to its dir<br>then open cmd & type </p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">go install github.com/spf13/cobra/cobra</span><br></pre></td></tr></tbody></table></figure>
<p>then use it:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">cobra init mycalc</span><br></pre></td></tr></tbody></table></figure>
<p>Cobra-based application structure:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">▾ appName/</span><br><span class="line">    ▾ cmd/</span><br><span class="line">        root.go</span><br><span class="line">    main.go</span><br><span class="line">``` </span><br><span class="line">cobra main.go structure:</span><br><span class="line">``` bash</span><br><span class="line">package main</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">  <span class="string">"mycalc/cmd"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">func <span class="function"><span class="title">main</span></span>() {</span><br><span class="line">  cmd.Execute()</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>when you run it, it will reports error:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\bin\mycalc>go run main.go</span><br><span class="line">main.go:18:8: cannot find package <span class="string">"go-work/bin/my-calc/cmd"</span> <span class="keyword">in</span> any of:</span><br><span class="line">        c:\go\src\go-work\bin\my-calc\cmd (from <span class="variable">$GOROOT</span>)</span><br><span class="line">        G:\go-work\src\go-work\bin\my-calc\cmd (from <span class="variable">$GOPATH</span>)</span><br></pre></td></tr></tbody></table></figure>
<p>⚠️so you copy the whole package to $GOPATH, then </p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\bin\mycalc>go install mycalc</span><br><span class="line">G:\go-work\bin\mycalc>go run main.go</span><br><span class="line">Hello CLI</span><br><span class="line"></span><br><span class="line">G:\go-work\bin>mycalc</span><br><span class="line">Hello CLI</span><br></pre></td></tr></tbody></table></figure>
<p>the command in root.go is called rootCmd.</p>
<h3 id="the-cli-flow"><a href="#the-cli-flow" class="headerlink" title="the cli flow"></a>the cli flow</h3><ol>
<li>edit root.go<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">func <span class="function"><span class="title">init</span></span>() {</span><br><span class="line">     fmt.Println(<span class="string">"inside init"</span>)</span><br><span class="line">     cobra.OnInitialize(initConfig)</span><br><span class="line">...</span><br></pre></td></tr></tbody></table></figure></li>
<li>edit main.go<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">func <span class="function"><span class="title">main</span></span>() {</span><br><span class="line">     fmt.Println(<span class="string">"inside main"</span>)</span><br><span class="line">     cmd.Execute()</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
and it will shows this:<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\bin>mycalc</span><br><span class="line">inside init</span><br><span class="line">inside main</span><br><span class="line">inside initConfig</span><br><span class="line">inside initConfig</span><br><span class="line">Hello CLI</span><br></pre></td></tr></tbody></table></figure>
Should notice two types of flags: Persistent flags & Local flags</li>
</ol>
<h3 id="add-cli"><a href="#add-cli" class="headerlink" title="add cli:"></a>add cli:</h3><ol>
<li>add num<br>create a command named add, navigate to the project <figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\src\mycalc>cobra add add</span><br><span class="line">add created at G:\go-work\src\mycalc</span><br></pre></td></tr></tbody></table></figure>
rebuild the binary using go install mycalc and run mycalc add.<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\src\mycalc>go install mycalc</span><br></pre></td></tr></tbody></table></figure>

</li>
</ol>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\bin>mycalc add</span><br><span class="line">add called</span><br></pre></td></tr></tbody></table></figure>
<ol start="2">
<li>modify it to add a series of numbers<br>to add numbers, fisrt have to convert the string into int then returan the result.<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">import (</span><br><span class="line">    ...</span><br><span class="line">    <span class="string">"strconv"</span></span><br><span class="line">)</span><br></pre></td></tr></tbody></table></figure></li>
<li>inside the add.go, create a new addInt function.<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">func addInt(args []string) {</span><br><span class="line">    var sum int</span><br><span class="line">    // the first <span class="built_in">return</span> value is index of args, we can omit it using _ </span><br><span class="line">    <span class="keyword">for</span> _, ival := range args {</span><br><span class="line">    // strconv is the library used <span class="keyword">for</span> <span class="built_in">type</span> conversion, <span class="keyword">for</span> string</span><br><span class="line">    // to int conversion Atio method is udes.</span><br><span class="line">        itemp, err := strconv.Atoi(ival)</span><br><span class="line">        <span class="keyword">if</span> err != nil {</span><br><span class="line">            fmt.Println(err)</span><br><span class="line">        }</span><br><span class="line">        sum = sum + itemp</span><br><span class="line">    }</span><br><span class="line">    fmt.Printf(<span class="string">"Addition of numbers %s is %d, args, sum)</span></span><br><span class="line"><span class="string">}</span></span><br></pre></td></tr></tbody></table></figure></li>
<li>update RUN func<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">...</span><br><span class="line">addInt(args)</span><br><span class="line">...</span><br></pre></td></tr></tbody></table></figure></li>
<li>rebuild<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">G:\go-work\bin>mycalc add a bc 8</span><br><span class="line">strconv.Atoi: parsing <span class="string">"a"</span>: invalid syntax</span><br><span class="line">strconv.Atoi: parsing <span class="string">"bc"</span>: invalid syntax</span><br><span class="line">Addition of numbers [a bc 8] is 8</span><br></pre></td></tr></tbody></table></figure>
<h3 id="achieve-float-calc-by-using-flag"><a href="#achieve-float-calc-by-using-flag" class="headerlink" title="achieve float calc by using flag"></a>achieve float calc by using flag</h3></li>
<li>create a local flag in init</li>
</ol>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">//add.go</span><br><span class="line">func <span class="function"><span class="title">init</span></span>() {</span><br><span class="line">    rootCmd.AddCommand(addCmd)</span><br><span class="line">    addCmd.Flags().BoolP(<span class="string">"float"</span>, <span class="string">"f"</span>,<span class="literal">false</span>,<span class="string">"Add Floating Numbers"</span>)</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ol start="2">
<li>create a addFloat func in add.go<br>//add.go<br>func addFloat(args []string) {<br> var sum float64<br> for _, fval := range args {<pre><code>ftemp, err := strconv.ParseFloat(fval,64)
if err != nil {
    fmt.Println(err)
}
sum = sum + ftemp</code></pre> }<br> fmt.Printf(“sum of floating nums %s is %f”, args,sum)<br>}<figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">3. create a addFloat func in addCmd RUN func</span><br><span class="line">``` bash</span><br><span class="line">// add.go</span><br><span class="line">// addCmd</span><br><span class="line">Run: func(cmd *cobra.Command, args []string) {</span><br><span class="line">    // get the flag value, its default value is false</span><br><span class="line">    fstatus, _ := cmd.Flags().GetBool("float")</span><br><span class="line">    if fstatus {</span><br><span class="line">        addFloat(args)</span><br><span class="line">    } else {</span><br><span class="line">        addInt(args)</span><br><span class="line">    }</span><br><span class="line">},</span><br></pre></td></tr></tbody></table></figure>
<h3 id="select-even-numbers-amp-add-them"><a href="#select-even-numbers-amp-add-them" class="headerlink" title="select even numbers & add them"></a>select even numbers & add them</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">cobra add even</span><br></pre></td></tr></tbody></table></figure></li>
<li>init in even.go<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">//even.go</span><br><span class="line">func <span class="function"><span class="title">init</span></span>() {</span><br><span class="line">    addCmd.AddCommand(evenCmd)</span><br><span class="line">...</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure></li>
<li>update evenCmd struct variable’s RUN method<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">//even.go</span><br><span class="line">Run: func(cmd *bobra.Command, args []string) {</span><br><span class="line"></span><br><span class="line">    var evenSum int</span><br><span class="line">    <span class="keyword">for</span> _, ival := range args {</span><br><span class="line">        itemp, _ := strconv.Atoi(ival)</span><br><span class="line">        <span class="keyword">if</span> itemp%2 == 0 {</span><br><span class="line">            evenSum = evenSum + itemp</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    fmt.Printf(<span class="string">"the even addition of %s is %d"</span>, args, evenSum)</span><br><span class="line">},</span><br></pre></td></tr></tbody></table></figure>
<h3 id="select-old-numbers-amp-add-them"><a href="#select-old-numbers-amp-add-them" class="headerlink" title="select old numbers & add them"></a>select old numbers & add them</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">//odd.go</span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> itemp%2 != 0 </span><br><span class="line">...</span><br></pre></td></tr></tbody></table></figure>
</li>
</ol>
</body></html>]]></content>
      <categories>
        <category>golang</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>cobra</tag>
        <tag>cli</tag>
      </tags>
  </entry>
  <entry>
    <title>404 Not Found</title>
    <url>/2019/12/25/404/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><center>
俺已经删除这个页面了
你可以<a href="https://liuyinggogogo.com>" target="_blank" rel="noopener">点击此处</a>返回首页。
</center>

<blockquote class="blockquote-center">
    LiuYing
</blockquote></body></html>]]></content>
  </entry>
  <entry>
    <title>convertexceltodocx</title>
    <url>/2019/12/12/convertexceltodocx/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h2 id="Convert-excelfiles-to-docx-by-python-script"><a href="#Convert-excelfiles-to-docx-by-python-script" class="headerlink" title="Convert excelfiles to docx by python script"></a>Convert excelfiles to docx by python script</h2><h3 id="1-dump-your-mysql-database-by-sql-command"><a href="#1-dump-your-mysql-database-by-sql-command" class="headerlink" title="1. dump your mysql database by sql command"></a>1. dump your mysql database by sql command</h3><p>open sql file paste the content in the navicatformyssql, then export the filtered contents to xlsx format</p>
<h3 id="2-Convert-excel"><a href="#2-Convert-excel" class="headerlink" title="2. Convert excel"></a>2. Convert excel</h3><p>using vs studio run the python script, convert it to md.</p>
<h3 id="3-Convert-md"><a href="#3-Convert-md" class="headerlink" title="3. Convert md"></a>3. Convert md</h3><p>open cmd, navigate the target location of your excel files, run:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">pandoc xxx.md -o yyy.docx</span><br></pre></td></tr></tbody></table></figure>
</body></html>]]></content>
      <categories>
        <category>documentation</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>excel</tag>
        <tag>doc</tag>
      </tags>
  </entry>
  <entry>
    <title>regexp</title>
    <url>/2019/12/11/rege/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h2 id="Go-inputs-verification"><a href="#Go-inputs-verification" class="headerlink" title="Go inputs verification"></a>Go inputs verification</h2><h3 id="1-Numbers"><a href="#1-Numbers" class="headerlink" title="1. Numbers"></a>1. Numbers</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">getint, err := strconv.Atoi(r.Form.Get("num"))</span><br><span class="line">if err != nil {</span><br><span class="line">  // error occurs when convert to number, it may not a Number</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">// check range of number</span><br><span class="line">if getint > 100 {</span><br><span class="line">  // too big</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">or</span><br><span class="line"></span><br><span class="line">if m, _ := regexp.MatchString("^[0-9]+$", r.Form.Get("num")); !m {</span><br><span class="line">  return flase</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<h3 id="2-Chinese"><a href="#2-Chinese" class="headerlink" title="2. Chinese"></a>2. Chinese</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">if m, _ := rangexp.MatchString("^[\\x{4e00}-\\x{9fa5}]+$", r.Form.Get("Chinesecharactor")); !m {</span><br><span class="line">  return false</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="3-Letters"><a href="#3-Letters" class="headerlink" title="3. Letters"></a>3. Letters</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">if m, _ := rangexp.MatchString("^[a-zA-Z]+$", r.Form.Get("englishletter")); !m {</span><br><span class="line">  return flase</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="4-E-mail"><a href="#4-E-mail" class="headerlink" title="4. E-mail"></a>4. E-mail</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">if m, _ := regexp.MatchString(`^([\w\.\_]{2,10})`,r.Form.Get("mail")); !m {</span><br><span class="line">  fmt.Println("no")</span><br><span class="line">}else {</span><br><span class="line">  fmt.Println("yes")</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="5-Drop-down-List"><a href="#5-Drop-down-List" class="headerlink" title="5. Drop down List"></a>5. Drop down List</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line"><select name="fruit"></span><br><span class="line"><option value="apple">apple</option></span><br><span class="line"><option value="pear">pear</option></span><br><span class="line"><option value="banana">banana</option></span><br><span class="line"></select></span><br></pre></td></tr></tbody></table></figure>
<p>and use the following strategy to sanitize our input:</p>
<figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">slice := []string{"apple", "pear", "banana"}</span><br><span class="line"></span><br><span class="line">for _, v := range slice {</span><br><span class="line">  if v == r.Form.Get("fruit") {</span><br><span class="line">    return true</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line">return false</span><br></pre></td></tr></tbody></table></figure>

<h3 id="6-Radio-buttons"><a href="#6-Radio-buttons" class="headerlink" title="6. Radio buttons"></a>6. Radio buttons</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line"><input type = "radio" name="gender" value="1">Male</span><br><span class="line"><input type = "radio" name="gender" value="2">Female</span><br></pre></td></tr></tbody></table></figure>
<p>use the following code to validate the input:</p>
<figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">slice := []int{1,2}</span><br><span class="line"></span><br><span class="line">for _, v := range slice {</span><br><span class="line">  if v == r.Form.Get("gender") {</span><br><span class="line">    return true</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line">return false</span><br></pre></td></tr></tbody></table></figure>
<h3 id="7-Check-boxes"><a href="#7-Check-boxes" class="headerlink" title="7. Check boxes"></a>7. Check boxes</h3><figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line"><input type="checkbox" name = "interest" value = "football">football</span><br><span class="line"><input type="checkbox" name="interest" value="basketball">Basketball</span><br><span class="line"><input type="checkbox" name="interest" value="tennis">Tennis</span><br></pre></td></tr></tbody></table></figure>
<p>In this case, the sanitization is a little bit different to validating the button and check box inputs since here we get a slice from the check boxes.</p>
<figure class="highlight plain"><table><tbody><tr><td class="code"><pre><span class="line">slice :=[]string{"football","basketball","tennis"}</span><br><span class="line">a := Slice_diff(r.Form["interest"],slice)</span><br><span class="line">if a == nil {</span><br><span class="line">  return true</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">return false</span><br></pre></td></tr></tbody></table></figure>

<h3 id="8-Date-and-time"><a href="#8-Date-and-time" class="headerlink" title="8. Date and time"></a>8. Date and time</h3><p>Suppose you want users to input valid dates or times. Go has the time package for converting year, month and day to their corresponding times. After that, it’s easy to check it.</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">t := time.Date(2019, time.December, 10, 23, 0, 0, 0, time.UTC)</span><br><span class="line">fmt.Printf(<span class="string">"Go launched at %s\n"</span>, t.Local())</span><br></pre></td></tr></tbody></table></figure>
<p>After you have the time, you can use the time package for more operations, depending on needs.</p>
</body></html>]]></content>
      <categories>
        <category>go</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>rege</tag>
        <tag>verification</tag>
      </tags>
  </entry>
  <entry>
    <title>userinfo_interface</title>
    <url>/2019/11/28/userinfo-interface/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><h2 id="一般需要写5个文件：controller-model-service-serviceImpl-DAO"><a href="#一般需要写5个文件：controller-model-service-serviceImpl-DAO" class="headerlink" title="一般需要写5个文件：controller + model + service + serviceImpl + DAO"></a>一般需要写5个文件：controller + model + service + serviceImpl + DAO</h2><h3 id="1-controller"><a href="#1-controller" class="headerlink" title="1. controller"></a>1. controller</h3><p>  一般由@RequestMapping和@ResponseBody构成</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">@RequestMapping(value = <span class="string">"路径"</span>, method = RequestMethod.GET, produces = <span class="string">"名称/json;charset=utf-8"</span>)</span><br><span class="line">  @ResponseBody</span><br><span class="line">  public ApiResult appUserList(@RequestParam(value = <span class="string">"入参"</span>,defaultValue = <span class="string">""</span>) 入参类型 入参) <span class="comment">#多个入参并列添加</span></span><br><span class="line">  {ApiResult apiResult = new ApiResult();</span><br><span class="line">  PageList<模型model> result = 对应的service.appUserList(入参之间逗号相隔);</span><br><span class="line">  <span class="built_in">return</span> GenericApiResultUtil.getGenericOKResult(result);}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="2-model"><a href="#2-model" class="headerlink" title="2. model"></a>2. model</h3><p>   就是指定数据库的表名以及各栏位的名称，其次是一些get,set方法，模板:</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">@SqlTable(name = <span class="string">"表名"</span>)</span><br><span class="line">public class model名称(即该java文件名称) {</span><br><span class="line">  @SqlColumn(name = <span class="string">"栏位名"</span>)</span><br><span class="line">  private <span class="built_in">type</span> name;</span><br><span class="line">  public long get栏位名() {</span><br><span class="line">      <span class="built_in">return</span> 栏位名;</span><br><span class="line">  }</span><br><span class="line"></span><br><span class="line">  public UserInfoCache <span class="built_in">set</span>栏位名(<span class="built_in">type</span> 栏位名) {</span><br><span class="line">      this.栏位名 = 栏位名;</span><br><span class="line">      <span class="built_in">return</span> this;</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="3-service"><a href="#3-service" class="headerlink" title="3. service"></a>3. service</h3><p>  这里指service文件夹里的service.java文件(public interface)和service文件夹里嵌套的文件夹impl里的serviceImpl.java文件。<br>  interface模板：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">  public interface service名称 {</span><br><span class="line">  List<类型> 接口名();</span><br><span class="line">  void 接口名(model名称 方法);</span><br><span class="line">  PageList<model类名> 对应controller(参数类型 参数);</span><br><span class="line">  int 接口名(参数类型 参数);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="4-serviceImpl"><a href="#4-serviceImpl" class="headerlink" title="4. serviceImpl"></a>4. serviceImpl</h3><p>  是service的执行，其模板：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">@Service</span><br><span class="line">@Transactional(propagation = Propagation.REQUIRED)</span><br><span class="line">public class service名称Impl implements service名称 {</span><br><span class="line">  private final UserInfoCacheDao userInfoCacheDao; <span class="comment"># DAO层的撰写</span></span><br><span class="line">  public service名称Impl(UserInfoCacheDao userInfoCacheDao)</span><br><span class="line">  {this.userInfoCacheDao = userInfoCacheDao;}</span><br><span class="line">  @Override</span><br><span class="line">  public PageList<model> 对应controller(参数类型 参数)</span><br><span class="line">  {<span class="built_in">return</span> model名称Dao.接口名(参数类型 参数);}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="5-DAO"><a href="#5-DAO" class="headerlink" title="5. DAO"></a>5. DAO</h3><p>结构模型：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">@Component</span><br><span class="line">public class yournameDao extends baseDao<modelname>{</span><br><span class="line">public PageList<modelname> impl中方法的返回值(参数类型 参数)</span><br><span class="line">{String selectSql = <span class="string">"yoursql "</span>; <span class="comment"># 封号前要有空格</span></span><br><span class="line">SqlBuider whereBuilder = new SqlBuilder();</span><br><span class="line">whereBuilder.append(<span class="string">"where ...space "</span>).args(参数);</span><br><span class="line">SqlBuilder limitBuilder = new SqlBuilder();</span><br><span class="line">limitBuilder.append(<span class="string">"limit ?, ?"</span>).args((page - 1) * size, size);</span><br><span class="line">String whereSql = whereBuilder.toString();</span><br><span class="line">String countSql = <span class="string">"yoursql"</span> + whereBuilder.toString();</span><br><span class="line">List<yourmodel> list = ...querryForBeanList(selectSql + whereSql + limitBuilder.toString(), classname.class);</span><br><span class="line">int total = ...querryForObject(countSql, Integer.class);</span><br><span class="line">PageList<yourmodel> PageList = new PageList<>();</span><br><span class="line">pageList.setList(list)</span><br><span class="line">pageList.setTotal(total)</span><br><span class="line"><span class="built_in">return</span> pageList;</span><br><span class="line">}</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
</body></html>]]></content>
      <categories>
        <category>interface</category>
      </categories>
      <tags>
        <tag>DAO</tag>
        <tag>service</tag>
        <tag>controller</tag>
      </tags>
  </entry>
  <entry>
    <title>check nginx error log</title>
    <url>/2019/11/06/checknginxjindocker/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h3 id="check-error-log"><a href="#check-error-log" class="headerlink" title="check error log"></a>check error log</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ docker <span class="built_in">exec</span> -it mirrorname bash</span><br><span class="line"><span class="built_in">cd</span> /var/<span class="built_in">log</span>/nginx/</span><br><span class="line">/var/<span class="built_in">log</span>/nginx<span class="comment">#</span></span><br><span class="line">cat error.log</span><br></pre></td></tr></tbody></table></figure>
<p><img src="../../images/nginx.png" alt="Alt text"></p>
</body></html>]]></content>
      <categories>
        <category>nginx</category>
      </categories>
      <tags>
        <tag>nginx</tag>
        <tag>docker</tag>
        <tag>error</tag>
      </tags>
  </entry>
  <entry>
    <title>userinfo_interface</title>
    <url>/2019/09/28/http/</url>
    <content><![CDATA[<html><head></head><body></body></html><html><head></head><body><h2 id="HTTP请求包"><a href="#HTTP请求包" class="headerlink" title="HTTP请求包"></a>HTTP请求包</h2><p>Request包分为3部分，依次为Request line,Request header, body.<br>HTTP协议与服务器交互的请求方法：GET,POST,PUT,DELETE.查增改删。</p>
<h3 id="fiddler"><a href="#fiddler" class="headerlink" title="fiddler"></a>fiddler</h3><p>GET方法<br><img src="https://raw.githubusercontent.com/astaxie/build-web-application-with-golang/master/zh/images/3.1.http.png" alt></p>
<p>POST方法<br><img src="https://raw.githubusercontent.com/astaxie/build-web-application-with-golang/master/zh/images/3.1.httpPOST.png" alt></p>
<h3 id="HTTP响应包（服务器信息）"><a href="#HTTP响应包（服务器信息）" class="headerlink" title="HTTP响应包（服务器信息）"></a>HTTP响应包（服务器信息）</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">HTTP/1.1 200 OK						//状态行</span><br><span class="line">Server: nginx/1.0.8					//服务器使用的WEB软件名及版本</span><br><span class="line">Date:Date: Tue, 30 Oct 2012 04:14:25 GMT		//发送时间</span><br><span class="line">Content-Type: text/html				//服务器发送信息的类型</span><br><span class="line">Transfer-Encoding: chunked			//表示发送HTTP包是分段发的</span><br><span class="line">Connection: keep-alive				//保持连接状态</span><br><span class="line">Content-Length: 90					//主体内容长度</span><br><span class="line">//空行 用来分割消息头和主体</span><br><span class="line"><!DOCTYPE html PUBLIC <span class="string">"-//W3C//DTD XHTML 1.0 Transitional//EN"</span>... //消息体</span><br></pre></td></tr></tbody></table></figure>
<p>Response包中的第一行叫做状态行，由HTTP协议版本号， 状态码， 状态消息 三部分组成。</p>
<p>状态码用来告诉HTTP客户端,HTTP服务器是否产生了预期的Response。HTTP/1.1协议中定义了5类状态码， 状态码由三位数字组成，第一个数字定义了响应的类别</p>
<ol>
<li>1XX 提示信息 - 表示请求已被成功接收，继续处理</li>
<li>2XX 成功 - 表示请求已被成功接收，理解，接受</li>
<li>3XX 重定向 - 要完成请求必须进行更进一步的处理</li>
<li>4XX 客户端错误 - 请求有语法错误或请求无法实现</li>
<li>5XX 服务器端错误 - 服务器未能实现合法的请求</li>
</ol>
<h3 id="HTTP协议是无状态的和Connection-keep-alive的区别"><a href="#HTTP协议是无状态的和Connection-keep-alive的区别" class="headerlink" title="HTTP协议是无状态的和Connection: keep-alive的区别"></a>HTTP协议是无状态的和Connection: keep-alive的区别</h3><p>无状态是指协议对于事务处理没有记忆能力，服务器不知道客户端是什么状态。从另一方面讲，打开一个服务器上的网页和你之前打开这个服务器上的网页之间没有任何联系。</p>
<p>HTTP是一个无状态的面向连接的协议，无状态不代表HTTP不能保持TCP连接，更不能代表HTTP使用的是UDP协议（面对无连接）。</p>
<p>从HTTP/1.1起，默认都开启了Keep-Alive保持连接特性，简单地说，当一个网页打开完成后，客户端和服务器之间用于传输HTTP数据的TCP连接不会关闭，如果客户端再次访问这个服务器上的网页，会继续使用这一条已经建立的TCP连接。</p>
<p>Keep-Alive不会永久保持连接，它有一个保持时间，可以在不同服务器软件（如Apache）中设置这个时间。</p>
</body></html>]]></content>
      <categories>
        <category>web</category>
      </categories>
      <tags>
        <tag>GET</tag>
        <tag>POST</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
</search>
